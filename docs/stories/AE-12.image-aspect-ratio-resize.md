---
Prioridade: P0
Sprint: Backlog
Assignee: "@dev (Dex)"
Revisao: "@architect"
Status: Draft
Epic: AE - Image Niche-Adaptive
Fase: Image Chain / Resize
Estimate: S
---

# Story AE-12 — Aspect ratio + overlay reserve por slot (resize com `sharp`)

## User Story

**Como** plataforma,
**quero** que cada slot de imagem (Welcome E1..E6, etc.) gere com o **aspect ratio correto** (4:5, 3:5, 4:3, 1:1) e com **terço inferior escuro reservado** para overlay quando aplicável,
**para que** o designer receba imagens prontas pro layout sem precisar recortar/redimensionar.

---

## Contexto

Hoje (apos AE-11):
- `image.chain.ts` chama OpenRouter com modelo `openai/gpt-5.4-image-2` e recebe base64
- Output: imagem com aspect ratio que o modelo decidir (historicamente landscape ~16:9 ou square)
- `DEFAULT_IMAGE_PROMPT_TEMPLATE` (legacy) menciona `600x300 landscape` no prompt — modelo ignora
- Template novo (AE-11 seed) usa vars `{{PALETA_1}}` etc, mas **NAO menciona aspect** — falta integrar

O plano (`/root/.claude/plans/muito-bom-agora-vamos-playful-marble.md` §3) define:

| Email | Aspect | Overlay reserve | Notas |
|---|---|---|---|
| Welcome E1 | 4:5 (1200×1500) | sim (terço inferior) | hero lifestyle |
| Welcome E2 | 4:5 | sim | produto + overlay grande |
| Welcome E3 | 3:5 (720×1200) | **não** | central vertical |
| Welcome E4 | 4:5 | sim | aspiracional |
| Welcome E5 | 4:3 (1200×900) | **não** | fachada |
| Welcome E6 | 4:5 | sim | high-impact |

**Estrategia segura**: instrução textual no prompt + **redimensionar pós-geração** usando `sharp` (resize com `fit: 'cover'` mantém composição).

---

## Acceptance Criteria

### AC AE-12.1 — Helper `aspect-ratio.ts`
- [ ] Arquivo `src/lib/agents/image/aspect-ratio.ts`
- [ ] Tipo `AspectKey = '4:5' | '3:5' | '4:3' | '1:1' | '3:4'`
- [ ] Funcao `getAspectDimensions(aspect: AspectKey): { width: number; height: number }`:
  - `'4:5'` → `{ width: 1200, height: 1500 }`
  - `'3:5'` → `{ width: 720, height: 1200 }`
  - `'4:3'` → `{ width: 1200, height: 900 }`
  - `'1:1'` → `{ width: 1200, height: 1200 }`
  - `'3:4'` → `{ width: 900, height: 1200 }`
- [ ] Funcao `resolveAspectForBlock(input: { blueprintAspect?: AspectKey | null; flowType?: string; emailNumber?: number }): AspectKey`:
  - Override do blueprint sempre vence
  - Senao matriz por `(flow_type, email_number)`: Welcome E3 → '3:5', E5 → '4:3', E1/E2/E4/E6 → '4:5'
  - Default `'4:5'`
- [ ] Funcao `aspectInstructionForPrompt(aspect: AspectKey, reserveBottom: boolean): string` — frase a injetar no prompt:
  - `"Render image at {aspect} aspect ratio (vertical 4:5 portrait composition)."`
  - Se reserveBottom: append `" Reserve the bottom 30% as a dark gradient/clean area for text overlay."`

### AC AE-12.2 — Adicionar `sharp` como dependencia
- [ ] `npm install sharp` (verificar primeiro se ja esta no package.json — story diz que NAO esta)
- [ ] Atualizar `package-lock.json`
- [ ] Lib `sharp` resolve resize de buffer PNG

### AC AE-12.3 — Estender `image.chain.ts`
- [ ] `generateEmailImage(prompt, storeId, options?)` agora aceita opcional:
  ```ts
  options?: {
    aspect?: AspectKey
    overlayReserveBottom?: boolean
  }
  ```
- [ ] Antes de fazer upload pro Supabase Storage:
  - Se `options.aspect` definido: `sharp(buffer).resize(width, height, { fit: 'cover', position: 'center' }).png().toBuffer()`
  - Senao: usa o buffer original (compat retroativa)
- [ ] Aspect ratio é tambem mencionado **no prompt** (string textual injetada pelo caller — phase2-runner)
- [ ] Loga `image.resize` com `{ from: { w, h }, to: { w, h } }` quando aplica

### AC AE-12.4 — Integracao em `phase2-runner.service.ts`
- [ ] Antes de chamar `generateEmailImage`:
  - Resolve aspect via `resolveAspectForBlock({ blueprintAspect: blueprint?.image_aspect, flowType, emailNumber })`
  - Resolve `overlayReserveBottom` via `blueprint?.image_overlay_reserve_bottom ?? true` (default true)
  - Append `aspectInstructionForPrompt(aspect, overlayReserveBottom)` no prompt renderizado (a frase entra DEPOIS do template renderizado, antes do envio)
  - Passa `{ aspect, overlayReserveBottom }` no `options` do `generateEmailImage`
- [ ] Loga `phase2.image.aspect_resolved` com `{ aspect, reserveBottom, source: 'blueprint' | 'matrix' | 'default' }`

### AC AE-12.5 — Testes
- [ ] `aspect-ratio.test.ts` (10+ testes):
  - `getAspectDimensions` 5 aspects
  - `resolveAspectForBlock` blueprint override
  - `resolveAspectForBlock` matrix welcome E1=4:5, E3=3:5, E5=4:3
  - `resolveAspectForBlock` flow desconhecido → default 4:5
  - `aspectInstructionForPrompt` com/sem reserveBottom
- [ ] `image.chain.test.ts` (criar se nao existir, ou estender):
  - Mock OpenRouter retornando PNG buffer 800x800
  - Com `options.aspect='4:5'`: chama sharp resize, output é 1200x1500
  - Sem options: passa buffer raw
  - Mock sharp via vi.mock pra evitar processamento real

### AC AE-12.6 — Compat retroativa
- [ ] Sem options ou sem blueprint: comportamento atual preservado (sem resize)
- [ ] Outros AE testes verdes (sem regressao)
- [ ] Typecheck OK

---

## Tarefas

- [ ] Criar `src/lib/agents/image/aspect-ratio.ts` + `.test.ts`
- [ ] `npm install sharp` + atualizar `package-lock.json`
- [ ] Estender `src/lib/agents/chains/image.chain.ts` com resize via sharp
- [ ] Modificar `src/lib/agents/phase2-runner.service.ts` (resolve aspect + injeta instrucao no prompt + passa options)
- [ ] Criar `src/lib/agents/chains/image.chain.test.ts` (se nao existir)

---

## Dev Notes

### Por que `sharp` e nao redimensionar no modelo?
- OpenRouter `image_size` historicamente eh ignorado por `gpt-5.4-image-2`
- `sharp` eh stdard na industria (Vercel + Next ja suportam), ~5MB extra no bundle
- Resize `fit: 'cover'` mantem composicao centralizada — modelo geralmente centraliza o produto

### Por que instrucao textual TAMBEM no prompt?
- Aspect ratio influencia composicao do modelo mesmo quando ele nao respeita a dimensao final
- "Reserve bottom 30%" eh respeitado pelo modelo na **composicao** (deixa area mais escura), e o resize preserva isso
- Sem a instrucao + reserve, modelo pode centralizar o produto perfeitamente — o overlay cobre

### Quando aplicar resize?
- **Antes do upload pro Supabase Storage**
- Buffer ja em memoria, custo extra ~50-100ms
- Output sempre PNG (perde tamanho mas mantem qualidade)

### Matriz de aspect ratio

```ts
const WELCOME_ASPECT_BY_EMAIL: Record<number, AspectKey> = {
  1: '4:5', 2: '4:5', 3: '3:5', 4: '4:5', 5: '4:3', 6: '4:5',
}
// Outros flows: default '4:5' ate que stories futuras definam
```

---

## Reuso de padroes existentes

- `sharp` é dep nova — mas amplamente usada (compat com Vercel build)
- `generateEmailImage` ja existe em `image.chain.ts` (AE-3 reusou)
- `phase2-runner.service.ts` ja tem loop de geracao por bloco
- `logger.child("Phase2Runner")`

---

## File List

### A criar
- `src/lib/agents/image/aspect-ratio.ts`
- `src/lib/agents/image/aspect-ratio.test.ts`
- `src/lib/agents/chains/image.chain.test.ts` (se nao existir)

### A modificar
- `src/lib/agents/chains/image.chain.ts` (assinatura aceita `options`, resize via sharp antes do upload)
- `src/lib/agents/phase2-runner.service.ts` (resolve aspect + injeta instrucao + passa options)
- `package.json` + `package-lock.json` (add `sharp`)

---

## Dependencias

- **Bloqueado por**: AE-10 (colunas `image_aspect`, `image_overlay_reserve_bottom` em email_blueprints)
- **Bloqueia**: AE-13 (PRODUCT-REF assume aspect ratio resolvido), AE-16 (UI regenera com aspect correto)

---

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|---------------|-----------|
| `sharp` quebra build Vercel | Baixa | Verificar `nodejs_runtime`; sharp eh suportado padrao Vercel |
| Resize degrada qualidade | Baixa | `fit: 'cover'` mantem produto centralizado; PNG output |
| Modelo ignora aspect instruction → resize trunca produto | Media | `position: 'center'` no resize protege caso comum; pra AE-14 ajustar prompts mestres pra serem mais explicitos |
| Custo +50-100ms por imagem | Baixa | Aceitavel; total fase 2 e 30-90s |

---

## Change Log

| Data | Autor | Descricao |
|------|-------|-----------|
| 2026-05-30 | @architect | Story criada apos AE-11 |
