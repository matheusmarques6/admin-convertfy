# Story 9.4 - Avatar Upload

**Prioridade:** P1
**Sprint:** Next
**Assignee:** @dev
**Revisao:** @sm (River)
**Status:** Pending
**Epic:** 9 - Configuracoes de Conta e Perfil
**Fase:** 2 - Data Integrity & Polish

---

## Contexto

Os usuarios nao podem fazer upload de foto de perfil. O dashboard layout (`src/app/(dashboard)/layout.tsx` linha ~178) ja le `profile.avatar_url` e renderiza o componente Avatar do shadcn, mas como nao existe forma de fazer upload, o campo eh sempre null e o fallback de iniciais eh exibido.

Existem patterns de upload no projeto: `src/app/api/upload/store-files/route.ts` e `src/app/api/public/upload/route.ts` que podem servir como referencia.

---

## Objetivo

1. Criar bucket Supabase Storage para avatares com RLS
2. Criar endpoints de upload e delete de avatar
3. Construir componente de UI para upload com preview circular

---

## Acceptance Criteria

- [ ] **9.4.1** Migration criada: bucket `avatars` no Supabase Storage:
  - Public read (avatares sao exibidos na UI)
  - Limite de 2MB por arquivo
  - Tipos permitidos: `image/jpeg`, `image/png`, `image/webp`
  - RLS policies:
    - INSERT: usuario pode fazer upload apenas em seu proprio folder (`{user_id}/`)
    - UPDATE: usuario pode sobrescrever apenas seu proprio avatar
    - DELETE: usuario pode deletar apenas seu proprio avatar
    - SELECT: publico (para exibir avatar)

- [ ] **9.4.2** Endpoint `POST /api/settings/avatar` criado:
  - Aceita FormData com campo `file`
  - Validacao server-side do tipo de arquivo (verificar magic bytes, NAO confiar apenas em Content-Type)
  - Validacao server-side do tamanho (max 2MB)
  - Upload para `avatars/{user_id}/avatar.{ext}` (sobrescreve anterior)
  - Atualiza `profiles.avatar_url` com URL publica do Storage
  - Retorna `{ avatar_url: string }`
  - Usa `createClient()` (user RLS)

- [ ] **9.4.3** Endpoint `DELETE /api/settings/avatar` criado:
  - Remove arquivo do Storage
  - Seta `profiles.avatar_url` para null
  - Retorna `{ success: true }`
  - Usa `createClient()` (user RLS)

- [ ] **9.4.4** Componente de avatar upload implementado:
  - Preview circular (reutilizar Avatar + AvatarFallback do shadcn)
  - Hover overlay com icone de camera e texto "Alterar foto"
  - Click abre file picker (input type="file" hidden)
  - Loading spinner durante upload
  - Preview otimista: mostra imagem selecionada imediatamente (URL.createObjectURL)
  - Botao de remover (icone X) quando avatar existe

- [ ] **9.4.5** Avatar integrado na pagina de perfil:
  - Posicionado acima do campo nome no formulario
  - Tamanho grande (96x96px ou similar)
  - Apos upload, sidebar e header refletem novo avatar (ja leem `avatar_url`)

- [ ] **9.4.6** Tratamento de erros:
  - Toast de erro se arquivo > 2MB ("Arquivo muito grande. Maximo 2MB")
  - Toast de erro se tipo invalido ("Formato nao suportado. Use JPG, PNG ou WebP")
  - Toast de sucesso ao fazer upload ("Foto atualizada")
  - Toast de sucesso ao remover ("Foto removida")

- [ ] **9.4.7** Run `npm run lint` passa sem erros
- [ ] **9.4.8** Run `npm run typecheck` passa sem erros

---

## Arquitetura Tecnica

### Migration: Storage Bucket

```sql
-- Criar bucket via Supabase Dashboard ou SQL
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'avatars',
  'avatars',
  true,
  2097152, -- 2MB
  ARRAY['image/jpeg', 'image/png', 'image/webp']
)
ON CONFLICT (id) DO NOTHING;

-- RLS Policies
CREATE POLICY "Users can upload own avatar"
  ON storage.objects FOR INSERT
  WITH CHECK (
    bucket_id = 'avatars'
    AND (storage.foldername(name))[1] = auth.uid()::text
  );

CREATE POLICY "Users can update own avatar"
  ON storage.objects FOR UPDATE
  USING (
    bucket_id = 'avatars'
    AND (storage.foldername(name))[1] = auth.uid()::text
  );

CREATE POLICY "Users can delete own avatar"
  ON storage.objects FOR DELETE
  USING (
    bucket_id = 'avatars'
    AND (storage.foldername(name))[1] = auth.uid()::text
  );

CREATE POLICY "Public avatar read access"
  ON storage.objects FOR SELECT
  USING (bucket_id = 'avatars');
```

### Endpoint POST /api/settings/avatar

```typescript
// src/app/api/settings/avatar/route.ts
import { createClient } from "@/lib/supabase/server";
import { NextResponse } from "next/server";

const ALLOWED_TYPES = ["image/jpeg", "image/png", "image/webp"];
const MAX_SIZE = 2 * 1024 * 1024; // 2MB

// Magic bytes para validacao
const MAGIC_BYTES: Record<string, number[]> = {
  "image/jpeg": [0xFF, 0xD8, 0xFF],
  "image/png": [0x89, 0x50, 0x4E, 0x47],
  "image/webp": [0x52, 0x49, 0x46, 0x46], // RIFF
};

export async function POST(request: Request) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });

  const formData = await request.formData();
  const file = formData.get("file") as File | null;
  if (!file) return NextResponse.json({ error: "No file provided" }, { status: 400 });

  // Validar tamanho
  if (file.size > MAX_SIZE) {
    return NextResponse.json({ error: "File too large. Max 2MB" }, { status: 400 });
  }

  // Validar tipo (Content-Type + magic bytes)
  if (!ALLOWED_TYPES.includes(file.type)) {
    return NextResponse.json({ error: "Invalid file type" }, { status: 400 });
  }

  const buffer = Buffer.from(await file.arrayBuffer());
  const isValidMagic = validateMagicBytes(buffer, file.type);
  if (!isValidMagic) {
    return NextResponse.json({ error: "Invalid file content" }, { status: 400 });
  }

  // Upload (sobrescreve anterior)
  const ext = file.type.split("/")[1] === "jpeg" ? "jpg" : file.type.split("/")[1];
  const path = `${user.id}/avatar.${ext}`;

  const { error: uploadError } = await supabase.storage
    .from("avatars")
    .upload(path, buffer, {
      contentType: file.type,
      upsert: true,
    });

  if (uploadError) {
    return NextResponse.json({ error: "Upload failed" }, { status: 500 });
  }

  // URL publica
  const { data: { publicUrl } } = supabase.storage
    .from("avatars")
    .getPublicUrl(path);

  // Atualizar profiles.avatar_url
  await supabase
    .from("profiles")
    .update({ avatar_url: publicUrl })
    .eq("id", user.id);

  return NextResponse.json({ avatar_url: publicUrl });
}
```

### Componente Avatar Upload

```typescript
// Dentro da pagina de perfil ou componente separado

function AvatarUpload({ avatarUrl, onUpload, onRemove }: AvatarUploadProps) {
  const fileInputRef = useRef<HTMLInputElement>(null);
  const [preview, setPreview] = useState<string | null>(null);
  const [isUploading, setIsUploading] = useState(false);

  // Click -> file picker
  // onChange -> preview otimista + POST /api/settings/avatar
  // Remove -> DELETE /api/settings/avatar

  return (
    <div className="relative group">
      <Avatar className="h-24 w-24">
        <AvatarImage src={preview || avatarUrl} />
        <AvatarFallback>{initials}</AvatarFallback>
      </Avatar>
      {/* Hover overlay com camera icon */}
      {/* Hidden file input */}
      {/* Remove button */}
    </div>
  );
}
```

---

## Ordem de Implementacao

1. [ ] Criar migration para bucket `avatars` com RLS policies
2. [ ] Criar `src/app/api/settings/avatar/route.ts` com POST handler (upload)
3. [ ] Adicionar DELETE handler no mesmo arquivo (remover)
4. [ ] Implementar componente AvatarUpload com preview circular e hover overlay
5. [ ] Integrar componente na pagina de perfil (acima do campo nome)
6. [ ] Testar upload JPG, PNG, WebP + validacao de tamanho
7. [ ] Run lint + typecheck

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `supabase/migrations/YYYYMMDDHHMMSS_create_avatars_bucket.sql` | CRIAR | Bucket + RLS policies |
| `src/app/api/settings/avatar/route.ts` | CRIAR | Endpoints POST (upload) e DELETE (remover) |
| `src/app/(dashboard)/settings/profile/page.tsx` | MODIFICAR | Integrar componente AvatarUpload |

---

## Dependencias

- **Depende de:** 9.3 (profiles schema atualizado, embora avatar_url ja exista)
- **Depende de:** 9.1 (pagina de perfil ja convertida para client component)
- **Referencia:** `src/app/api/upload/store-files/route.ts`, `src/app/api/public/upload/route.ts`

---

## Riscos e Mitigacoes

| Risco | Mitigacao |
|-------|-----------|
| Supabase Storage pode nao estar habilitado no projeto | Verificar configuracao do Storage antes de implementar |
| Magic bytes validation pode ser incompleta para WebP | WebP usa RIFF header; validar primeiros 4 bytes + "WEBP" em offset 8-11 |
| Avatar antigo em formato diferente nao eh deletado ao re-upload | Usar `upsert: true` para sobrescrever; considerar cleanup de extensoes diferentes |
| URL publica com cache do CDN pode mostrar imagem antiga | Adicionar query param de cache bust (`?t={timestamp}`) na URL |

---

## CodeRabbit Integration

### Quality Gates
- [ ] Lint clean
- [ ] Typecheck clean
- [ ] Magic bytes validation presente (nao apenas Content-Type)
- [ ] Storage RLS impede upload em folder de outro usuario
- [ ] File size validated server-side (nao apenas client-side)

### Specialized Agents
- **@dev**: Implementacao
- **@qa**: Testar upload com arquivo invalido (ex: .exe renomeado para .jpg)

### Review Focus Areas
- Validacao de magic bytes alem do Content-Type
- RLS policy usa `storage.foldername(name)` para isolamento por user_id
- `upsert: true` para sobrescrever avatar anterior
- Preview otimista com `URL.createObjectURL` para UX instantanea
- Cleanup do object URL para evitar memory leak

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-02-28 | Story criada | @sm (River) |
