# Story 20.1 — Zod Schemas para Campaign Generation

## Metadados

| Campo      | Valor                                        |
|------------|----------------------------------------------|
| Prioridade | P0                                           |
| Sprint     | Atual                                        |
| Assignee   | @dev                                         |
| Revisao    | @sm                                          |
| Status     | Done                                        |
| Epic       | 20 - Gerador de Campanhas (Copy Generation)  |
| Fase       | Schemas                                      |

---

## Story

**Como** desenvolvedor implementando as APIs de campaign generation,
**Quero** schemas Zod compartilhados para validacao de request/response,
**Para que** todas as rotas usem validacao consistente e tipada.

---

## Contexto

Os testes TDD em `src/lib/schemas/campaign-generation.test.ts` definem exatamente os schemas necessarios. Este arquivo deve ser criado ANTES das API routes (stories 20.3-20.5) pois todas importam dele.

Os schemas definem:
1. `generateRequestSchema` — body do POST /api/campaigns/generate
2. `webhookCallbackSchema` — body do POST /api/campaigns/webhook-callback

---

## Acceptance Criteria

### 20.1.1 — Criar generateRequestSchema

- [x] Criar arquivo `src/lib/schemas/campaign-generation.ts`
- [x] Exportar `generateRequestSchema` como `z.object({...})`
- [x] Campo `name`: `z.string().min(1).max(255)` — obrigatorio
- [x] Campo `date`: `z.string().regex(/^\d{4}-\d{2}-\d{2}$/)` — formato YYYY-MM-DD
- [x] Campo `reference_doc_url`: `z.string().url().nullable().optional()` — URL valida ou null
- [x] Campo `store_ids`: `z.array(z.string().uuid()).min(1)` — pelo menos uma loja, UUIDs
- [x] Campo `generation_id`: `z.string().uuid().nullable().optional()` — para regeneracao

### 20.1.2 — Criar webhookCallbackSchema

- [x] Exportar `webhookCallbackSchema` como `z.object({...})`
- [x] Campo `generation_id`: `z.string().uuid()` — obrigatorio
- [x] Campo `drive_folder_id`: `z.string().optional()`
- [x] Campo `drive_folder_url`: `z.string().url().optional()`
- [x] Campo `stores`: array de objetos com `store_id` (uuid), `status` (enum "done"|"error"), `error_message` (string optional)

### 20.1.3 — Exportar tipos TypeScript inferidos

- [x] Exportar `type GenerateRequest = z.infer<typeof generateRequestSchema>`
- [x] Exportar `type WebhookCallbackPayload = z.infer<typeof webhookCallbackSchema>`

### 20.1.4 — Todos os testes TDD passando

- [x] Executar `npx vitest run src/lib/schemas/campaign-generation.test.ts`
- [x] Todos os 18 testes devem passar:
  - generateRequestSchema (12): accepts valid request with all fields, accepts without optional fields, rejects empty name, rejects invalid date format (4 formats), rejects empty store_ids, rejects non-UUID store_ids, accepts null reference_doc_url, rejects invalid reference_doc_url, accepts null generation_id, accepts multiple store_ids, rejects name > 255 chars, accepts duplicate store_ids
  - webhookCallbackSchema (6): accepts all done, accepts partial errors, rejects invalid generation_id, rejects invalid store status, accepts without drive info, rejects missing generation_id

---

## Arquitetura Tecnica

### Arquivo a criar

```
src/lib/schemas/campaign-generation.ts
```

### Codigo esperado (baseado nos testes)

```typescript
import { z } from "zod"

export const generateRequestSchema = z.object({
  name: z.string().min(1, "Nome da campanha e obrigatorio").max(255, "Nome da campanha muito longo"),
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, "Data deve estar no formato YYYY-MM-DD"),
  reference_doc_url: z.string().url().nullable().optional(),
  store_ids: z.array(z.string().uuid()).min(1, "Selecione pelo menos uma loja"),
  generation_id: z.string().uuid().nullable().optional(),
})

export const webhookCallbackSchema = z.object({
  generation_id: z.string().uuid("generation_id deve ser um UUID valido"),
  drive_folder_id: z.string().optional(),
  drive_folder_url: z.string().url().optional(),
  stores: z.array(z.object({
    store_id: z.string().uuid(),
    status: z.enum(["done", "error"]),
    error_message: z.string().optional(),
  })),
})

export type GenerateRequest = z.infer<typeof generateRequestSchema>
export type WebhookCallbackPayload = z.infer<typeof webhookCallbackSchema>
```

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `src/lib/schemas/campaign-generation.ts` | CRIAR | Schemas Zod + tipos TS |

## Dependencias

- Nenhuma (primeira story do epic)

## Riscos

- Nenhum — schemas sao puros, sem side effects

## Testes de Referencia

- `src/lib/schemas/campaign-generation.test.ts` — 18 testes

## CodeRabbit Integration

### Specialized Agents
- **Schema Validation Agent**: Verificar consistencia dos schemas com os testes

### Review Focus Areas
- Schemas exportados corretamente
- Tipos inferidos exportados
- Mensagens de erro em portugues onde aplicavel

---

## Definition of Done

- [x] Typecheck passa: `npm run typecheck`
- [x] Lint passa: `npm run lint`
- [x] 18 testes passam: `npx vitest run src/lib/schemas/campaign-generation.test.ts`

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-03-05 | Story criada | @sm |
| 2026-03-05 | Fix contagem testes (16->18), add DoD | @sm |
