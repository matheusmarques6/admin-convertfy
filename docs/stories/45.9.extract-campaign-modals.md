# Story 45.9 — Extract Campaign Modals (Detail + Day List)

## Status: Ready for Review
## Effort: MEDIUM (1-2h)
## Priority: MEDIUM

## Context

Two modals are defined inline in `page.tsx`:
1. **Campaign Detail Modal** — shows full campaign info + Klaviyo metrics (150+ lines)
2. **Day Campaigns List Modal** — shows list of campaigns for a clicked day with quick actions

These should be standalone components with typed props.

## Acceptance Criteria

- [x] **AC 45.9.1** `CampaignDetailModal` extracted to `src/components/campaigns/campaign-detail-modal.tsx`
- [x] **AC 45.9.2** Detail modal receives `campaign: PortalCampaign | null`, `metrics: PortalCampaignDetailMetrics | null`, `isLoading: boolean`, `onClose: () => void`
- [x] **AC 45.9.3** `CampaignDayListModal` extracted to `src/components/campaigns/campaign-day-list-modal.tsx`
- [x] **AC 45.9.4** Day list modal receives `date: string`, `campaigns: PortalCampaign[]`, `onSelect: (c: PortalCampaign) => void`, `onClose: () => void`
- [x] **AC 45.9.5** Handles `formatDistanceToNow` with `{ addSuffix: true, locale: ptBR }` instead of manual " atras" (fix i18n grammar)
- [x] **AC 45.9.6** No behavioral changes — same visual output
- [x] **AC 45.9.7** Lint + typecheck pass

## File List

- [x] `src/components/campaigns/campaign-detail-modal.tsx` — NEW
- [x] `src/components/campaigns/campaign-day-list-modal.tsx` — NEW
- [x] `src/app/client/campaigns/page.tsx` — import modals, remove inline code
