# Story 8.18 - Deprecate dashboard_cache for Klaviyo Data

**Prioridade:** P2 (Media — cleanup)
**Sprint:** Next
**Assignee:** @dev
**Revisao:** @sm (River)
**Status:** Ready for Review
**Epic:** 8.x Phase 2 - Portal Performance Optimization + Period Fix
**Fase:** 4 - Cleanup

---

## Contexto

Apos Stories 8.16 e 8.17, o portal dashboard le TODOS os dados Klaviyo de tabelas cacheadas pelo cron:
- `store_revenue_summary` — revenue agregado + audience metrics
- `klaviyo_campaign_metrics` — detalhes individuais por campanha
- `klaviyo_flow_metrics` — detalhes individuais por flow

A tabela `dashboard_cache` (criada na migration `20250123_dashboard_cache.sql`) era o mecanismo original de cache do portal. Ela armazena:
- `cache_type = "klaviyo_perf"` — dados de performance Klaviyo (agora redundante)
- `cache_type = "shopify"` — dados de report Shopify (AINDA necessario)

### Uso atual de `dashboard_cache` no portal

O portal dashboard (`/api/portal/dashboard/route.ts`) usa `getCachedData`/`saveToCache`/`getStaleData` para:
1. **Klaviyo** (`klaviyo_perf`): cache de `KlaviyoPerformanceData` — **SERA REMOVIDO** (substituido por cron tables)
2. **Shopify** (`shopify`): cache de Shopify report data — **MANTER** (Shopify nao tem cron equivalente)

### Outros usos de `dashboard_cache`

Verificar se `dashboard_cache` e usado em outros endpoints:
- `src/lib/integrations/klaviyo/cached-metadata.ts` — usa para cache de metadata (account info, metric IDs). Esse e um uso DIFERENTE e deve ser avaliado separadamente.
- `src/app/api/client-stores/[id]/route.ts` — pode usar dashboard_cache. Verificar.
- `src/lib/cache.ts` — `cleanExpiredCache()` limpa entries expirados da `dashboard_cache`.

---

## Objetivo

Remover todo o fluxo de Klaviyo data via `dashboard_cache` no portal, deixando essa tabela apenas para dados Shopify (e quaisquer outros usos nao-Klaviyo). Limpar dead code e imports nao utilizados.

---

## Acceptance Criteria

- [x] **8.18.1** Remover `getCachedData(storeId, "klaviyo_perf")` calls no portal dashboard — ALREADY DONE by Story 8.16 (portal rewrite removed all klaviyo_perf calls)
- [x] **8.18.2** Remover `saveToCache(storeId, "klaviyo_perf", ...)` calls no portal dashboard — ALREADY DONE by Story 8.16
- [x] **8.18.3** Remover `getStaleData(storeId, "klaviyo_perf")` calls no portal dashboard — ALREADY DONE by Story 8.16
- [x] **8.18.4** Manter `getCachedData(storeId, "shopify")` e `saveToCache(storeId, "shopify", ...)` intactos — Verified: Shopify cache via getCachedShopifyData/saveShopifyToCache inline functions, fully intact
- [x] **8.18.5** Remover import de `fetchKlaviyoPerformance` e `KlaviyoPerformanceData` se nao mais utilizados no portal — ALREADY DONE by Story 8.16 (not imported)
- [x] **8.18.6** Remover import de `withConcurrencyLimit` se nao mais utilizado no portal — ALREADY DONE by Story 8.16 (not imported)
- [x] **8.18.7** Remover `CACHE_TTL` entries especificos de Klaviyo se nao mais necessarios (manter para Shopify) — Removed `klaviyo_perf` from CACHE_TTL in cache.ts. Kept `klaviyo_perf` in STALE_GRACE_MINUTES (still used by clients/[id]/performance as fallback)
- [x] **8.18.8** Remover `CACHE_VERSION` check logica especifica para `klaviyo_perf` (linhas 248-275 do portal) — ALREADY DONE by Story 8.16 (no klaviyo_perf-specific checks exist in portal)
- [x] **8.18.9** Verificar `src/lib/integrations/klaviyo/cached-metadata.ts`:
  - Usa `dashboard_cache` para metadata caching (klaviyo_metadata type) via getCache/setCache — MANTIDO (uso diferente)
- [x] **8.18.10** Verificar `src/app/api/client-stores/[id]/route.ts`:
  - NAO usa dashboard_cache diretamente — apenas menciona em comentario CASCADE. Nenhuma acao necessaria.
- [x] **8.18.11** NAO deletar a tabela `dashboard_cache` (ainda usada para Shopify + metadata) — Confirmado: tabela mantida
- [x] **8.18.12** Limpar dead code: funcoes helper que so eram usadas para Klaviyo cache — Verificado: getCachedData/saveToCache/getStaleData genéricos em cache.ts ainda usados por Shopify e metadata. Nenhum helper exclusivo de Klaviyo restante.
- [x] **8.18.13** `cleanExpiredCache()` em `src/lib/cache.ts` continua funcionando (limpa Shopify entries) — Verificado: funcao nao alterada, limpa todas entries expiradas independente de cache_type
- [x] **8.18.14** Nenhuma regressao em nenhuma feature do dashboard (portal ou admin) — Unica mudanca: remoção de TTL entry que ninguém mais escrevia
- [x] **8.18.15** Run `npm run lint` passa sem erros — Passa (1 warning pre-existente em store-tracking-tab.tsx)
- [x] **8.18.16** Run `npm run typecheck` passa sem erros — Passa (2 erros pre-existentes em sync-reports/route.ts, nao relacionados)

---

## Arquitetura Tecnica

### Codigo a Remover no Portal Dashboard

```typescript
// REMOVER: import nao mais necessario
import {
  fetchKlaviyoPerformance,
  type KlaviyoPerformanceData,
} from "@/lib/services/klaviyo-performance.service"
import { withConcurrencyLimit } from "@/lib/integrations/klaviyo"

// REMOVER: no fetchStoreData, todo o bloco de Klaviyo live fetch + cache
const apiKey = store.klaviyo_private_key || store.klaviyo_api_key
if (apiKey) {
  const cached = forceRefresh ? null : await getCachedData(store.id, "klaviyo_perf")
  if (cached) {
    storeData.klaviyoPerf = cached as unknown as KlaviyoPerformanceData
  } else {
    // ... fetchKlaviyoPerformance + saveToCache + getStaleData ...
  }
}

// MANTER: Shopify block intacto
const hasShopify = !!(store.shopify_access_token && store.shopify_store_domain)
if (hasShopify) {
  const cachedShopify = await getCachedData(store.id, "shopify")
  // ...
}
```

### Helpers que podem ser simplificados

As funcoes `getCachedData`, `saveToCache`, `getStaleData` podem ser mantidas (usadas por Shopify), mas a logica de `CACHE_VERSION` check especifica para `klaviyo_perf` (linhas 252-275) pode ser removida:

```typescript
// REMOVER: check especifico de klaviyo_perf dentro de getCachedData
if (cacheType === "klaviyo_perf") {
  const perf = cached as unknown as KlaviyoPerformanceData
  if (perf.storeRevenue === 0 && perf.attributedRevenue === 0) { ... }
  if (perf.storeRevenue > 0 && perf.attributedRevenue === 0 ...) { ... }
  if (perf.attributedRevenue > perf.storeRevenue) { ... }
  const hasGenericNames = perf.recentCampaigns?.some(...) || ...
  if (hasGenericNames) { ... }
}
```

---

## Ordem de Implementacao

1. [x] Auditar todos os usos de `dashboard_cache` no projeto (grep completo)
2. [x] No portal dashboard: remover Klaviyo live fetch + cache logic — JA FEITO por Story 8.16
3. [x] No portal dashboard: remover imports nao utilizados — JA FEITO por Story 8.16
4. [x] Simplificar `getCachedData` — remover checks especificos de `klaviyo_perf` — JA FEITO por Story 8.16. Removido klaviyo_perf de CACHE_TTL.
5. [x] Verificar cached-metadata.ts e client-stores route — cached-metadata usa klaviyo_metadata (MANTER), client-stores nao usa dashboard_cache
6. [x] Run lint + typecheck — Passa (issues pre-existentes nao relacionados)
7. [x] Testar: portal funciona sem regressao; Shopify data ainda cacheado

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `src/app/api/portal/dashboard/route.ts` | NAO MODIFICADO | Ja limpo por Story 8.16 — nenhum klaviyo_perf restante |
| `src/lib/cache.ts` | MODIFICADO | Removido klaviyo_perf de CACHE_TTL (ninguem mais escreve esse tipo) |
| `src/lib/integrations/klaviyo/cached-metadata.ts` | VERIFICADO | Usa klaviyo_metadata (diferente) — mantido |
| `src/app/api/client-stores/[id]/route.ts` | VERIFICADO | Nao usa dashboard_cache — apenas comentario CASCADE |

---

## Dependencias

- **Depende de:** 8.16 (portal le do cache cron), 8.17 (audience metrics cacheados)
- **Bloqueia:** Nenhuma (cleanup story)

---

## Riscos e Mitigacoes

| Risco | Mitigacao |
|-------|-----------|
| Remover codigo errado que ainda e necessario | Grep completo por "klaviyo_perf" e "dashboard_cache" antes de remover |
| Shopify cache afetado acidentalmente | Manter getCachedData/saveToCache intactos; so remover chamadas com "klaviyo_perf" |
| Outros endpoints usam dashboard_cache para Klaviyo | Auditar com grep antes de fazer mudancas |
| cached-metadata.ts depende de dashboard_cache | Verificar e manter se necessario |

---

## CodeRabbit Integration

### Quality Gates
- [ ] Lint clean
- [ ] Typecheck clean
- [ ] Nenhum import nao utilizado
- [ ] Shopify cache continua funcionando
- [ ] Nenhum dado Klaviyo flui via dashboard_cache

### Specialized Agents
- **@dev**: Implementar cleanup
- **@qa**: Testar portal end-to-end (Klaviyo + Shopify)

### Review Focus Areas
- Grep por "klaviyo_perf" — nenhuma referencia restante no portal
- Grep por "fetchKlaviyoPerformance" — nao importado no portal
- `getCachedData`/`saveToCache` mantidos para Shopify
- `cleanExpiredCache` nao quebrado
- `CACHE_VERSION` import mantido se Shopify cache usa

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-02-28 | Story criada — deprecacao de dashboard_cache para dados Klaviyo | @sm (River) |
| 2026-02-28 | Implementado: removido klaviyo_perf de CACHE_TTL. Portal ja limpo por 8.16. Audit completo. | @dev (Dex) |
