# Story 13.1 - Criar Componente StoresListModal

**Prioridade:** P2 (Enhancement)
**Sprint:** Current
**Assignee:** @dev
**Revisao:** @sm (River)
**Status:** Ready for Review
**Epic:** 13 - Ver Todas as Lojas (Modal com Paginacao)
**Fase:** 1 - Componente

---

## Contexto

Os cards Top Lojas e Atencao Necessaria mostram apenas 5 lojas. Quando ha mais de 5, o usuario nao tem como ver as demais. Precisamos de um modal compartilhado que exiba a lista completa com paginacao client-side (10 por pagina).

O projeto ja usa o componente `Dialog` do shadcn/ui (`src/components/ui/dialog.tsx`) e o tipo `RevenueStoreItem` exportado de `total-revenue-banner.tsx`. O modal sera 100% client-side, sem novas chamadas de API.

---

## Objetivo

Criar o componente `StoresListModal` em `src/components/dashboard/stores-list-modal.tsx` como modal reutilizavel que:
- Recebe um array de `RevenueStoreItem[]` e uma `variant` ("top" | "bottom")
- Exibe 10 lojas por pagina com paginacao client-side
- Usa cores verde (top) ou vermelho (bottom) conforme a variant
- Cada loja e um link clicavel para `/stores/{storeId}`

---

## Acceptance Criteria

- [x] **13.1.1** Criar arquivo `src/components/dashboard/stores-list-modal.tsx` com o componente `StoresListModal`

- [x] **13.1.2** O componente deve aceitar as seguintes props:
  ```typescript
  interface StoresListModalProps {
    stores: RevenueStoreItem[]
    variant: "top" | "bottom"
    open: boolean
    onOpenChange: (open: boolean) => void
  }
  ```

- [x] **13.1.3** O titulo do Dialog deve ser:
  - Variant "top": icone `Trophy` (verde) + "Top Lojas — Todas"
  - Variant "bottom": icone `TrendingDown` (vermelho) + "Atencao Necessaria — Todas"

- [x] **13.1.4** Cada item da lista deve exibir:
  - Numero de posicao (1., 2., 3...) continuo entre paginas (pagina 2 comeca no 11.)
  - Nome da loja (truncado se longo)
  - Receita total formatada com moeda correta via `formatCurrency(store.totalRevenue, store.currency || "BRL")`
  - Barra de progresso relativa ao primeiro item da lista (mesmo pattern dos cards atuais)
  - Detalhes de Campaign + Flow revenue para todas as lojas (nao apenas top 2 como nos cards)
  - Cada item e um `Link` para `/stores/${store.storeId}` (fecha o modal ao navegar)

- [x] **13.1.5** Paginacao client-side com 10 itens por pagina:
  - Usar `useMemo` para calcular `totalPages` e `paginatedStores`
  - Botoes "Anterior" e "Proximo" com estado `disabled` nas extremidades
  - Indicador "Pagina X de Y" centralizado
  - Resetar para pagina 1 quando `stores` array mudar (novo periodo selecionado)

- [x] **13.1.6** Layout responsivo:
  - `DialogContent` com `className="max-w-lg max-h-[80vh]"`
  - Lista com `overflow-y-auto` para scroll quando necessario
  - Paginacao fixa no footer do dialog

- [x] **13.1.7** Cores da barra de progresso por variant:
  - "top": `bg-gradient-to-r from-emerald-500 to-emerald-400` (mesmo do `top-stores-card.tsx`)
  - "bottom": `bg-gradient-to-r from-red-500 to-orange-400` (mesmo do `worst-performers-card.tsx`)

- [x] **13.1.8** Cor do valor da receita por variant:
  - "top": `text-foreground` (padrao)
  - "bottom": `text-destructive`

- [x] **13.1.9** Estado vazio: se `stores.length === 0`, mostrar mensagem "Nenhuma loja encontrada" centralizada

- [x] **13.1.10** Run `npm run lint` passa sem erros
- [x] **13.1.11** Run `npm run typecheck` passa sem erros

---

## Arquitetura Tecnica

### Arquivo: `src/components/dashboard/stores-list-modal.tsx` (NOVO)

```typescript
"use client"

import { useState, useMemo, useEffect } from "react"
import Link from "next/link"
import { Trophy, TrendingDown, ChevronLeft, ChevronRight } from "lucide-react"
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { formatCurrency } from "@/lib/utils"
import type { RevenueStoreItem } from "./total-revenue-banner"

const ITEMS_PER_PAGE = 10

interface StoresListModalProps {
  stores: RevenueStoreItem[]
  variant: "top" | "bottom"
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function StoresListModal({ stores, variant, open, onOpenChange }: StoresListModalProps) {
  const [page, setPage] = useState(1)

  // Reset to page 1 when stores change (e.g. period change)
  useEffect(() => {
    setPage(1)
  }, [stores])

  const totalPages = Math.max(1, Math.ceil(stores.length / ITEMS_PER_PAGE))
  const paginatedStores = useMemo(
    () => stores.slice((page - 1) * ITEMS_PER_PAGE, page * ITEMS_PER_PAGE),
    [stores, page]
  )

  const maxRevenue = stores[0]?.totalRevenueBRL ?? stores[0]?.totalRevenue ?? 1
  const isTop = variant === "top"

  const barGradient = isTop
    ? "bg-gradient-to-r from-emerald-500 to-emerald-400"
    : "bg-gradient-to-r from-red-500 to-orange-400"

  const revenueColor = isTop ? "text-foreground" : "text-destructive"

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-lg max-h-[80vh] flex flex-col">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2 text-base">
            {isTop ? (
              <Trophy className="h-4 w-4 text-success" />
            ) : (
              <TrendingDown className="h-4 w-4 text-destructive" />
            )}
            {isTop ? "Top Lojas — Todas" : "Atencao Necessaria — Todas"}
          </DialogTitle>
        </DialogHeader>

        {stores.length === 0 ? (
          <p className="text-sm text-muted-foreground text-center py-8">
            Nenhuma loja encontrada
          </p>
        ) : (
          <>
            {/* Scrollable list */}
            <div className="flex-1 overflow-y-auto space-y-3 pr-1">
              {paginatedStores.map((store, i) => {
                const globalIndex = (page - 1) * ITEMS_PER_PAGE + i
                const storeRevBRL = store.totalRevenueBRL ?? store.totalRevenue
                const widthPercent = Math.max((storeRevBRL / maxRevenue) * 100, 4)
                const curr = store.currency || "BRL"
                return (
                  <Link
                    key={store.storeId}
                    href={`/stores/${store.storeId}`}
                    className="block group"
                    onClick={() => onOpenChange(false)}
                  >
                    <div className="flex items-center justify-between text-xs mb-1">
                      <span className="truncate max-w-[55%] text-foreground group-hover:text-primary transition-colors">
                        <span className="text-muted-foreground mr-1.5">
                          {globalIndex + 1}.
                        </span>
                        {store.storeName}
                      </span>
                      <span className={`font-medium tabular-nums ${revenueColor}`}>
                        {formatCurrency(store.totalRevenue, curr)}
                      </span>
                    </div>
                    <div className="h-1.5 rounded-full bg-muted overflow-hidden">
                      <div
                        className={`h-full rounded-full ${barGradient} transition-all duration-500`}
                        style={{ width: `${widthPercent}%` }}
                      />
                    </div>
                    <p className="text-[10px] text-muted-foreground mt-0.5">
                      Campaign: {formatCurrency(store.campaignRevenue, curr)} · Flow: {formatCurrency(store.flowRevenue, curr)}
                    </p>
                  </Link>
                )
              })}
            </div>

            {/* Pagination footer */}
            {totalPages > 1 && (
              <div className="flex items-center justify-between pt-3 border-t border-border">
                <Button
                  variant="outline"
                  size="sm"
                  disabled={page <= 1}
                  onClick={() => setPage(p => p - 1)}
                >
                  <ChevronLeft className="h-3.5 w-3.5 mr-1" />
                  Anterior
                </Button>
                <span className="text-xs text-muted-foreground">
                  Pagina {page} de {totalPages}
                </span>
                <Button
                  variant="outline"
                  size="sm"
                  disabled={page >= totalPages}
                  onClick={() => setPage(p => p + 1)}
                >
                  Proximo
                  <ChevronRight className="h-3.5 w-3.5 ml-1" />
                </Button>
              </div>
            )}
          </>
        )}
      </DialogContent>
    </Dialog>
  )
}
```

### Decisoes de Design

1. **Paginacao inline** ao inves de componente separado: feature especifica, nao justifica componente generico
2. **`maxRevenue` usa o primeiro item da lista COMPLETA** (nao da pagina): barra proporcional consistente entre paginas
3. **`onClick={() => onOpenChange(false)}`** no Link: fecha o modal ao navegar para a loja
4. **`useEffect` para resetar pagina**: quando o usuario muda o periodo no PeriodPicker, os stores mudam e o modal volta para pagina 1

---

## Ordem de Implementacao

1. [x] Criar arquivo `stores-list-modal.tsx` com a estrutura base
2. [x] Implementar layout do Dialog com header e variant
3. [x] Implementar lista de lojas com barra de progresso
4. [x] Implementar paginacao client-side
5. [x] Implementar estado vazio
6. [x] Run lint + typecheck

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `src/components/dashboard/stores-list-modal.tsx` | CRIAR | Componente modal compartilhado com paginacao |

---

## Dependencias

- **Depende de:** Nenhuma (pode ser implementada imediatamente)
- **Bloqueia:** 13.3 (integracao nos cards precisa deste componente)

---

## Riscos e Mitigacoes

| Risco | Mitigacao |
|-------|-----------|
| Lista longa sem virtualizacao | Maximo de ~50 lojas por org — nao justifica react-window. Se no futuro escalar, virtualizar |
| `maxRevenue` = 0 causa divisao por zero | Guard `?? 1` ja presente na conta do widthPercent |
| Modal fecha ao navegar mas Next.js prefetch pode ser lento | `onClick` fecha imediatamente, navegacao e independente |
| Ultima pagina com poucos itens | Layout flex + space-y-3 funciona com 1-10 itens sem quebrar |

---

## CodeRabbit Integration

### Quality Gates
- [x] Lint clean
- [x] Typecheck clean
- [x] Componente exportado corretamente
- [x] Props tipadas sem `any`

### Specialized Agents
- **@dev**: Implementar componente
- **@qa**: Testar edge cases (0 lojas, 5 lojas, 11 lojas, moedas mistas)

### Review Focus Areas
- Verificar que `formatCurrency` recebe a moeda correta (nao hardcoded BRL)
- Verificar acessibilidade do Dialog (focus trap, ESC para fechar, aria-modal)
- Confirmar que `maxRevenue` usa a lista completa, nao a pagina atual

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-03-04 | Story criada — componente modal com paginacao client-side | @sm (River) |
