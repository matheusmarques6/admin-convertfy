---
Prioridade: P0
Sprint: Backlog
Assignee: "@dev (Dex)"
Revisao: "@architect"
Status: In Review
Epic: AE - Image Niche-Adaptive
Fase: DB / Helpers
Estimate: M
---

# Story AE-10 — Schema niche-adaptive + helpers de derivação

## User Story

**Como** arquiteto do sistema,
**quero** estender `email_blueprints` com campos de briefing de imagem por slot, criar `store_image_overrides` para overrides manuais e introduzir os 4 helpers puros de derivação,
**para que** as stories AE-11 a AE-15 possam ler/escrever esse schema e renderizar prompts niche-adaptive.

---

## Contexto

Decisão arquitetural em `/root/.claude/plans/muito-bom-agora-vamos-playful-marble.md` (plano aprovado).

O sistema novo precisa de 12 variáveis (`{MARCA}, {LOGO_STYLE}, {NICHO}, {PRODUTO_HEROI}, {PUBLICO}, {CENARIO}, {PALETA_1}, {PALETA_2}, {NEUTRO}, {MOOD}, {IDIOMA}, {MOEDA}`). Hoje:

- `{MARCA}, {NICHO}, {PUBLICO}, {IDIOMA}, {MOEDA}, {PALETA_1}, {PALETA_2}` ja saem direto do schema atual (`client_stores`, `store_briefings`, `store_brand_identities`).
- `{MOOD}` precisa virar 3 palavras (hoje e enum 4 valores).
- `{CENARIO}` precisa ser derivado de `{NICHO} x posicionamento_preco`.
- `{NEUTRO}` precisa heuristica de cor neutra.
- `{LOGO_STYLE}` precisa derivacao por fonte.
- `{PRODUTO_HEROI}` ja temos via `store_top_products[rank=1]`, mas e necessario permitir override por loja (caso top product nao sirva).

E precisa de 4 novas colunas em `email_blueprints` para briefing de imagem por slot (`image_brief`, `image_aspect`, `image_mode`, `image_overlay_reserve_bottom`, `image_produto_heroi_hint`).

---

## Acceptance Criteria

### AC AE-10.1 — Migration aplica sem erros em prod
- [x] Arquivo `supabase/migrations/20260601_image_agent_niche_adaptive.sql` existe
- [ ] Roda 2x sem erro (idempotencia: `IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`) — validar em staging
- [x] Adiciona 5 colunas em `email_blueprints`: `image_brief TEXT`, `image_aspect TEXT DEFAULT '4:5'`, `image_mode TEXT DEFAULT 'auto'`, `image_overlay_reserve_bottom BOOLEAN DEFAULT true`, `image_produto_heroi_hint TEXT`
- [x] CHECK constraints corretos para `image_aspect` (`4:5,3:5,4:3,1:1,3:4`) e `image_mode` (`auto,product_ref,text2img`) — com guards `DO $$ ... EXCEPTION duplicate_object`
- [x] Cria tabela `store_image_overrides` com 7 colunas + RLS + policy `authenticated_full_access`

### AC AE-10.2 — Tipos TS atualizados
- [x] `src/types/email-generation.ts`: tipo `EmailBlueprint` ganha as 5 novas colunas opcionais
- [x] Novo tipo `StoreImageOverrides` em `src/types/email-generation.ts`

### AC AE-10.3 — Helper `mood-mapping.ts`
- [x] Arquivo: `src/lib/agents/image/mood-mapping.ts`
- [x] Funcao: `mapTomVozToMood(tomVoz: string | null | undefined): string` (retorna 3 palavras separadas por virgula)
- [x] Mapeamento implementado conforme story (formal/casual/afetivo/divertido + default `"clean, modern, balanced"`)
- [x] Export do tipo `MoodKey`

### AC AE-10.4 — Helper `cenario-derivation.ts`
- [x] Arquivo: `src/lib/agents/image/cenario-derivation.ts`
- [x] Funcao: `deriveCenario(input: { nicho, posicionamento, override? }): string`
- [x] Override sempre vence (strings vazias/whitespace são descartadas)
- [x] Tabela `NICHO_CENARIO_MAP` cobre 13 chaves (moda, beleza, automotivo, pet, gourmet, decoracao, tech, gadgets, saude, suplementos, infantil, esporte, fitness)
- [x] Cruzamento por substring case-insensitive; fallback `"ambiente clean e contemporaneo"` para nicho desconhecido
- [x] Posicionamento adiciona sufixo (premium → "sofisticado e exclusivo", popular → "acessivel e cotidiano", medio → vazio)

### AC AE-10.5 — Helper `neutro-resolution.ts`
- [x] Arquivo: `src/lib/agents/image/neutro-resolution.ts`
- [x] Funcao: `resolveNeutro(input): string` retorna hex
- [x] Logica: override (não-vazio) > role='Fundo' (case-insensitive, requer hex) > posicionamento (premium=#0B0B0B / popular=#F5F5F5 / default=#FFFFFF)

### AC AE-10.6 — Helper `logo-style.ts`
- [x] Arquivo: `src/lib/agents/image/logo-style.ts`
- [x] Funcao: `deriveLogoStyle(input): string`
- [x] override > regex case-insensitive: serif/slab/old-style/mincho → classic monogram; mono/courier/consolas → tech monospace; display/bold/impact/black → bold display lettering; default → modern wordmark

### AC AE-10.7 — Testes unit
- [x] `mood-mapping.test.ts`: 9 testes (4 enums + default + null + undefined + empty + normalização)
- [x] `cenario-derivation.test.ts`: 10 testes (override, vazio, 3 posicionamentos, substring, tech/gadgets, fallback, null/undefined, fallback+premium)
- [x] `neutro-resolution.test.ts`: 9 testes (override, vazio, role=Fundo primary/secondary, heurística premium/popular/default, null arrays, Fundo sem hex)
- [x] `logo-style.test.ts`: 9 testes (override, vazio, serif/slab/mono/display, sans default, null/undefined/empty, case-insensitive)
- [x] 37/37 testes passam; 100% dos branches cobertos

### AC AE-10.8 — `buildImagePromptVars` estendido
- [x] Assinatura aceita `{ emailNumber?, flowType?, blueprint?, storeOverrides? }` opcionais
- [x] Retorna superset: 19 vars antigas (snake_case) + 12 vars MAIUSCULAS (`MARCA, LOGO_STYLE, NICHO, PRODUTO_HEROI, PUBLICO, CENARIO, PALETA_1, PALETA_2, NEUTRO, MOOD, IDIOMA, MOEDA`)
- [x] Compat retroativa: chamada sem novos params retorna todas as 31 vars (antigas funcionais + niche-adaptive com fallbacks dos helpers)
- [x] Usa `mapTomVozToMood` (MOOD), `deriveCenario` (CENARIO), `resolveNeutro` (NEUTRO), `deriveLogoStyle` (LOGO_STYLE)
- [x] `PRODUTO_HEROI`: override loja > `blueprint.image_produto_heroi_hint` > `topProducts[0].name`
- [x] `IDIOMA` fixo `"pt-BR"`; `MOEDA` vem de `storeRaw.currency` (fallback `"BRL"`)

---

## Tarefas

- [x] Criar `supabase/migrations/20260601_image_agent_niche_adaptive.sql`
- [x] Estender tipos em `src/types/email-generation.ts` (`EmailBlueprint` + novo `StoreImageOverrides`)
- [x] Criar 4 helpers em `src/lib/agents/image/`
- [x] Criar 4 arquivos de teste correspondentes (37 testes verdes)
- [x] Estender `buildImagePromptVars` em `src/lib/agents/email-generation.service.ts` (mantendo compat)

---

## Dev Notes

- **Nao tocar em `image.chain.ts` nesta story** — fica para AE-12.
- **Nao seedear `email_agent_configs.image`** — fica para AE-11 (template renderer).
- **Nao tocar nos blueprints existentes** — UPDATE de E1-E6 fica para AE-14.
- Migration adiciona apenas COLUNAS e TABELA NOVA — zero risco de regressao.
- Helpers sao puros (sem DB) — testes rapidos.

---

## File List

### A criar
- `supabase/migrations/20260601_image_agent_niche_adaptive.sql`
- `src/lib/agents/image/mood-mapping.ts` + `.test.ts`
- `src/lib/agents/image/cenario-derivation.ts` + `.test.ts`
- `src/lib/agents/image/neutro-resolution.ts` + `.test.ts`
- `src/lib/agents/image/logo-style.ts` + `.test.ts`

### A modificar
- `src/types/email-generation.ts` (`EmailBlueprint` ganha 5 colunas + tipo `StoreImageOverrides`)
- `src/lib/agents/email-generation.service.ts` (`buildImagePromptVars` estendido)

---

## Dependencias

- **Bloqueado por**: nenhum
- **Bloqueia**: AE-11, AE-12, AE-13, AE-14, AE-15

---

## Change Log

| Data | Autor | Descricao |
|------|-------|-----------|
| 2026-05-30 | @architect | Story criada a partir do plano niche-adaptive |
| 2026-05-30 | @dev (Claude) | Implementado: migration + 4 helpers puros + extensão de `buildImagePromptVars`. 37/37 testes verdes, typecheck OK, lint OK. Pendente: validar migration rodando 2x em staging (AC AE-10.1 sub-item). Status → In Review. |
