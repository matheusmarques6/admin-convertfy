# Story 10.1 - Shared Period Constants + DataStatus Type

**Prioridade:** P0 (Critica — fundacao para todas as demais stories)
**Sprint:** Current
**Assignee:** @dev
**Revisao:** @sm (River)
**Status:** Ready for Review
**Epic:** 10 - Cache-First + Live Fallback Unificado
**Fase:** 1 - Fundacao

---

## Contexto

O projeto tem 3 problemas de inconsistencia que afetam todo o epic:

1. **DataStatus nao padronizado (M4):** O campo `dataStatus` e usado de forma ad-hoc em diferentes endpoints:
   - `src/app/api/dashboard/total-revenue/route.ts` usa `"ready" | "syncing"` (linhas 31-32)
   - `src/app/api/portal/dashboard/route.ts` usa `"ready" | "syncing"` (linhas 554, 658)
   - Nao existe um type compartilhado nem campos auxiliares (`lastFetchedAt`, `isRefreshing`)

2. **Periodos desalinhados (M5):** O cron define `PERIODS = ["30d", "7d", "15d", "90d"]` (linha 24 do cron), mas o portal aceita qualquer periodo incluindo `"1d"`, `"12m"`, `"custom"`. Nao existe uma constante que defina quais periodos tem cache pre-populado vs quais precisam de live fetch.

3. **Resultado de sync nao tipado (M3-parcial):** `klaviyoRequest()` retorna `null` silenciosamente em qualquer erro (400, 429, timeout). Os sync services precisam de um tipo de resultado explicito: `{ success: boolean; data: T | null; error?: string }`.

---

## Objetivo

Criar modulo compartilhado `src/lib/shared/data-status.ts` com types e constantes que serao usados por todos os endpoints e services do Epic 10.

---

## Acceptance Criteria

- [x] **10.1.1** Criar arquivo `src/lib/shared/data-status.ts` com o type `DataStatus`:
  ```typescript
  export type DataStatus = "loading" | "stale" | "ready" | "error" | "empty"
  ```

- [x] **10.1.2** Criar interface `DataStatusMeta` no mesmo arquivo:
  ```typescript
  export interface DataStatusMeta {
    dataStatus: DataStatus
    lastFetchedAt: string | null
    isRefreshing: boolean
    source: "cache" | "live" | "stale-cache"
  }
  ```

- [x] **10.1.3** Criar constantes de periodos compartilhadas:
  ```typescript
  /** Periodos que o cron pre-popula — cache sempre disponivel apos primeiro sync */
  export const CACHED_PERIODS = ["7d", "15d", "30d", "90d"] as const
  export type CachedPeriod = typeof CACHED_PERIODS[number]

  /** Periodos que requerem live fetch (nao cacheados pelo cron) */
  export const LIVE_ONLY_PERIODS = ["1d", "12m", "custom"] as const
  export type LiveOnlyPeriod = typeof LIVE_ONLY_PERIODS[number]

  /** Todos os periodos validos */
  export const ALL_PERIODS = [...CACHED_PERIODS, ...LIVE_ONLY_PERIODS] as const
  export type Period = typeof ALL_PERIODS[number]

  /** Checagem utilitaria */
  export function isCachedPeriod(period: string): period is CachedPeriod {
    return (CACHED_PERIODS as readonly string[]).includes(period)
  }
  ```

- [x] **10.1.4** Criar type generico para resultados de sync service (M3):
  ```typescript
  export interface SyncResult<T> {
    success: boolean
    data: T | null
    error?: string
    source: "cache" | "live"
    fetchedAt: string
  }
  ```

- [x] **10.1.5** Criar constantes de cooldown compartilhadas:
  ```typescript
  /** Cooldown em ms entre live fetches para o mesmo store+period */
  export const LIVE_FETCH_COOLDOWN_MS = 60_000 // 1 minuto

  /** Cooldown para custom date ranges (mais curto pois nao tem cron) */
  export const CUSTOM_RANGE_COOLDOWN_MS = 30 * 60_000 // 30 minutos

  /** TTL de cache para resultados de live fetch */
  export const LIVE_FETCH_CACHE_TTL_MS = 5 * 60_000 // 5 minutos
  ```

- [x] **10.1.6** Atualizar `src/app/api/cron/sync-reports/route.ts` para importar `CACHED_PERIODS` em vez de definir `PERIODS` localmente:
  ```typescript
  // ANTES (linha 24):
  const PERIODS = ["30d", "7d", "15d", "90d"] as const

  // DEPOIS:
  import { CACHED_PERIODS } from "@/lib/shared/data-status"
  // ... e substituir PERIODS por CACHED_PERIODS no codigo
  ```

- [x] **10.1.7** Atualizar `src/app/api/dashboard/total-revenue/route.ts` para usar `DataStatus` type:
  ```typescript
  // ANTES (linha 31):
  dataStatus?: "ready" | "syncing"

  // DEPOIS:
  import { DataStatus } from "@/lib/shared/data-status"
  dataStatus?: DataStatus
  ```

- [x] **10.1.8** Atualizar `src/app/api/portal/dashboard/route.ts` para usar `DataStatus` type nos campos `response.dataStatus`

- [x] **10.1.9** Run `npm run lint` passa sem erros
- [x] **10.1.10** Run `npm run typecheck` passa sem erros

---

## Arquitetura Tecnica

### Novo Arquivo: `src/lib/shared/data-status.ts`

```typescript
/**
 * Shared types and constants for cache-first + live fallback strategy.
 * Used by cron, admin endpoints, portal endpoints, and frontend.
 *
 * Epic 10 - Cache-First + Live Fallback Unificado
 */

// ─── DataStatus ──────────────────────────────────────────────────────────────

/** Unified data status across all endpoints */
export type DataStatus = "loading" | "stale" | "ready" | "error" | "empty"

/** Metadata returned alongside data to inform frontend about freshness */
export interface DataStatusMeta {
  dataStatus: DataStatus
  lastFetchedAt: string | null
  isRefreshing: boolean
  /** Where the data came from */
  source: "cache" | "live" | "stale-cache"
}

// ─── Periods ─────────────────────────────────────────────────────────────────

/** Periods that the cron pre-populates — cache always available after first sync */
export const CACHED_PERIODS = ["7d", "15d", "30d", "90d"] as const
export type CachedPeriod = (typeof CACHED_PERIODS)[number]

/** Periods that require live fetch (not cached by cron) */
export const LIVE_ONLY_PERIODS = ["1d", "12m", "custom"] as const
export type LiveOnlyPeriod = (typeof LIVE_ONLY_PERIODS)[number]

/** All valid periods */
export const ALL_PERIODS = [...CACHED_PERIODS, ...LIVE_ONLY_PERIODS] as const
export type Period = (typeof ALL_PERIODS)[number]

/** Type guard: is this period pre-cached by cron? */
export function isCachedPeriod(period: string): period is CachedPeriod {
  return (CACHED_PERIODS as readonly string[]).includes(period)
}

// ─── Sync Result ─────────────────────────────────────────────────────────────

/** Generic result type for sync service operations (replaces silent null returns) */
export interface SyncResult<T> {
  success: boolean
  data: T | null
  error?: string
  source: "cache" | "live"
  fetchedAt: string
}

// ─── Cooldown Constants ──────────────────────────────────────────────────────

/** Cooldown in ms between live fetches for the same store+period */
export const LIVE_FETCH_COOLDOWN_MS = 60_000

/** Cooldown for custom date ranges (M2: key = store_id + date_range_hash) */
export const CUSTOM_RANGE_COOLDOWN_MS = 30 * 60_000

/** TTL for live fetch results cached in store_revenue_summary */
export const LIVE_FETCH_CACHE_TTL_MS = 5 * 60_000
```

### Impacto no Cron

Substituicao minima — apenas trocar a constante local `PERIODS` pela importada `CACHED_PERIODS`:

```typescript
// sync-reports/route.ts
import { CACHED_PERIODS } from "@/lib/shared/data-status"

// Na funcao syncStore (linha 576):
// ANTES: periods: readonly string[]
// DEPOIS: periods: readonly string[] (sem mudanca de tipo, so da constante)

// No GET handler (linha 740):
// ANTES: log.info(`... × ${PERIODS.length} periods`)
// DEPOIS: log.info(`... × ${CACHED_PERIODS.length} periods`)
```

---

## Ordem de Implementacao

1. [x] Criar `src/lib/shared/data-status.ts` com todos os types e constantes
2. [x] Atualizar cron para importar `CACHED_PERIODS`
3. [x] Atualizar `total-revenue/route.ts` para usar `DataStatus`
4. [x] Atualizar `portal/dashboard/route.ts` para usar `DataStatus`
5. [x] Run lint + typecheck
6. [ ] Verificar que cron, admin dashboard e portal continuam funcionando

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `src/lib/shared/data-status.ts` | CRIAR | Types, constantes e helpers compartilhados |
| `src/app/api/cron/sync-reports/route.ts` | MODIFICAR | Importar CACHED_PERIODS, remover PERIODS local |
| `src/app/api/dashboard/total-revenue/route.ts` | MODIFICAR | Usar DataStatus type |
| `src/app/api/portal/dashboard/route.ts` | MODIFICAR | Usar DataStatus type |

---

## Dependencias

- **Depende de:** Nenhuma (fundacao)
- **Bloqueia:** 10.2, 10.3, 10.4, 10.5, 10.6, 10.7

---

## Riscos e Mitigacoes

| Risco | Mitigacao |
|-------|-----------|
| Substituir PERIODS no cron pode causar regressao | Constante tem os mesmos valores, apenas fonte diferente |
| DataStatus "loading" vs "syncing" (breaking change frontend) | "syncing" nao existe mais — frontend precisa atualizar (Story 10.7) |
| Importar de `@/lib/shared/` pode nao resolver | Verificar tsconfig paths — `@/lib/*` ja mapeia para `src/lib/*` |

---

## CodeRabbit Integration

### Quality Gates
- [x] Lint clean
- [x] Typecheck clean
- [x] CACHED_PERIODS tem os mesmos valores que PERIODS anterior
- [x] DataStatus type usado em todos os endpoints que retornam dataStatus

### Specialized Agents
- **@dev**: Implementar modulo compartilhado e atualizar imports
- **@qa**: Verificar que endpoints retornam dataStatus correto

### Review Focus Areas
- Types sao puramente aditivos — nenhuma logica de negocios muda
- Cron continua com os mesmos periodos na mesma ordem
- `isCachedPeriod()` sera usado extensivamente em Stories 10.5/10.6
- `SyncResult<T>` sera o return type de todos os sync services (Stories 10.2/10.3)

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-03-01 | Story criada — fundacao de types para Epic 10 | @sm (River) |
