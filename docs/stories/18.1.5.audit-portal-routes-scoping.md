# Story 18.1.5 -- P1-Step 2: Audit All Portal Routes Scoping

## Metadados

| Campo      | Valor                                              |
|------------|----------------------------------------------------|
| Prioridade | Alta                                               |
| Sprint     | Atual                                              |
| Assignee   | @dev                                               |
| Revisao    | @sm                                                |
| Status     | Ready for Review                                   |
| Epic       | 18 - Multi-Tenant Security Hardening               |
| Fase       | P1 - Defense-in-Depth (Auditoria + Codigo)         |

---

## Story

**Como** administrador do sistema,
**Quero** que TODAS as rotas portal sejam auditadas para scoping correto por client_id,
**Para que** nenhuma rota permita acesso cross-tenant, mesmo com adminClient.

---

## Contexto

Apos P0 (remocao de bypass policies), o risco principal e codigo que usa `adminClient` sem filtrar por `client_id`. Todas as rotas portal devem ser auditadas sistematicamente.

### Inventario completo de rotas portal

Baseado em analise do codigo-fonte, aqui esta o inventario completo de 22 rotas em `src/app/api/portal/`:

**AUDITORIA CONCLUIDA em 2026-03-05 por @dev.** Cada arquivo foi lido na integra. Resultado: 0 FIX NEEDED, 1 DEAD CODE, 32 OK.

| # | Rota | Client usado | Scoping | Status | Verificado |
|---|------|-------------|---------|--------|------------|
| 1 | `auth/route.ts` GET | adminClient | `.eq("auth_user_id", user.id)` via `client_portal_users` | OK | Yes |
| 2 | `auth/route.ts` POST | adminClient | `.eq("auth_user_id", authData.user.id)` + update `.eq("id", portalUser.id)` | OK | Yes |
| 3 | `auth/route.ts` DELETE | supabase | `auth.signOut()` — no data query | OK | Yes |
| 4 | `auth/verify/route.ts` POST | adminClient | `.eq("auth_user_id", userId)` + `userId === authUser.id` validated | OK | Yes |
| 5 | `branding/route.ts` GET | adminClient | `auth_user_id` -> `client_id` -> `org_id` chain | OK | Yes |
| 6 | `campaigns/route.ts` GET | adminClient | `client_id` -> `clientStoreIds` -> `.in("store_id", filterStoreIds)` | OK | Yes |
| 7 | `change-password/route.ts` POST | supabase + adminClient | SELECT via supabase (RLS `auth_user_id`), UPDATE via `adminClient.auth.admin.updateUserById` + `.eq("id", portalUser.id)` | OK | Yes |
| 8 | `dashboard/route.ts` GET | adminClient | `.eq("client_id", clientId)` on all queries (stores, cache, perf) | OK | Yes |
| 9 | `integrations/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` on `client_stores` | OK | Yes |
| 10 | `integrations/route.ts` PUT | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` on `client_stores` + store ownership | OK | Yes |
| 11 | `invoices/route.ts` GET | supabase (portal_users) + adminClient (data) | portalUser via supabase RLS, data via `.eq("client_id", portalUser.client_id)` + integrations via `.eq("org_id", clientData.org_id)` | OK | Yes |
| 12 | `onboarding/route.ts` GET | adminClient | `.eq("client_id", clientId)` on onboardings + `.eq("onboarding_id", onboarding.id)` on steps | OK | Yes |
| 13 | `onboarding/wizard/route.ts` GET | adminClient | `.eq("client_id", portalUser.client_id)` on all tables | OK | Yes |
| 14 | `onboarding/wizard/route.ts` POST | adminClient | `.eq("client_id", portalUser.client_id)` + store ownership verified per step | OK | Yes |
| 15 | `settings/route.ts` GET | supabase | `.eq("auth_user_id", user.id)` via RLS policy | OK | Yes |
| 16 | `settings/route.ts` PUT | supabase | `.eq("id", portalUser.id)` via RLS policy | OK | Yes |
| 17 | `settings/avatar/route.ts` POST | supabase | `.eq("id", portalUser.id)` via RLS policy + storage scoped by `user.id` | OK | Yes |
| 18 | `settings/avatar/route.ts` DELETE | supabase | `.eq("id", portalUser.id)` via RLS policy + storage scoped by `user.id` | OK | Yes |
| 19 | `settings/notifications/route.ts` PUT | supabase | Tabela `client_notification_preferences` NAO EXISTE (dead code). Comentario adicionado. | DEAD CODE | Yes |
| 20 | `settings/password/route.ts` PUT | supabase | `.eq("auth_user_id", user.id)` via RLS policy | OK | Yes |
| 21 | `stores/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` | OK | Yes |
| 22 | `stores/route.ts` PUT | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` + `.eq("id", store_id)` ownership | OK | Yes |
| 23 | `stores/onboarding/route.ts` POST | adminClient | `.eq("client_id", clientId)` + store ownership verified | OK | Yes |
| 24 | `stores/[id]/report/route.ts` GET | adminClient | `.eq("client_id", portalUser.client_id)` on stores | OK | Yes |
| 25 | `stores/[id]/utm-templates/route.ts` GET | adminClient | `.eq("client_id", portalUser.client_id)` on stores + `.eq("store_id", storeId)` + `.eq("org_id", store.org_id)` | OK | Yes |
| 26 | `tracking/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` on client_stores | OK | Yes |
| 27 | `tracking/route.ts` POST | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` + store ownership | OK | Yes |
| 28 | `tracking/config/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` on client_stores | OK | Yes |
| 29 | `tracking/config/route.ts` PUT | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` + `.in("client_store_id", clientStoreIds)` ownership | OK | Yes |
| 30 | `tracking/stores/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` on client_stores | OK | Yes |
| 31 | `tracking/stores/route.ts` POST | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` + store ownership | OK | Yes |
| 32 | `tracking/stores/route.ts` DELETE | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` + `.in("client_store_id", clientStoreIds)` | OK | Yes |
| 33 | `tracking/orders/route.ts` GET | adminClient (via `getPortalUser`) | `.eq("client_id", portalUser.client_id)` -> clientStoreIds -> trackingStoreIds | OK | Yes |

---

## Acceptance Criteria

### Auditoria

- [x] **AC 18.1.5.1** — Auditar CADA rota portal e confirmar scoping. Para cada rota, verificar:
  1. Usa `adminClient` ou `supabase`?
  2. Se `adminClient`: tem filtro `.eq("client_id", ...)` ou equivalente?
  3. Se `supabase`: a policy RLS cobre o acesso (ex: `auth_user_id = auth.uid()`)?
  4. Resultado: OK, FIX NEEDED, ou DEAD CODE

- [x] **AC 18.1.5.2** — Documentar resultado da auditoria na tabela acima (atualizar esta story)

### GAP-3: integrations bypass policy

- [x] **AC 18.1.5.3** — Documentar status da bypass policy de `integrations`:
  - Policy: `"Authenticated users can manage integrations"` com `USING(true)`
  - Status apos 18.1.1: invoices usa `adminClient` com filtro `org_id` (fixado)
  - Portal `integrations/route.ts` ja usa `adminClient` com `.eq("client_id", ...)` (OK)
  - Risco residual: qualquer usuario autenticado pode ler integracoes via Supabase JS client
  - Decisao: documentar como candidata para Story 18.2 (remediacoes admin-only)

### Dead code

- [x] **AC 18.1.5.4** — Documentar `settings/notifications/route.ts` como dead code:
  - Tabela `client_notification_preferences` NAO EXISTE no banco
  - Queries falham silenciosamente
  - NAO e risco de seguranca, mas e tech debt
  - Adicionar comentario `// TODO: Dead code — tabela client_notification_preferences nao existe` no topo do arquivo

### Rotas com supabase que dependem de policies

- [x] **AC 18.1.5.5** — Confirmar que as seguintes rotas continuam funcionando apos P0:
  - `settings/route.ts` GET/PUT: usa `supabase` -> `client_portal_users` com policy `auth_user_id`
  - `settings/avatar/route.ts` POST/DELETE: usa `supabase` -> `client_portal_users` com policy `auth_user_id`
  - `settings/password/route.ts` PUT: usa `supabase` -> `client_portal_users` com policy `auth_user_id`
  - `change-password/route.ts` POST: SELECT via `supabase` (policy OK), UPDATE via `adminClient` (OK)
  - Todas funcionam porque dependem da policy `"Portal users can view/update own record"` que filtra por `auth_user_id = auth.uid()` — esta policy NAO e removida em P0

---

## Tarefas

- [x] T1: Confirmar que P0 esta completo
- [x] T2: Percorrer CADA rota portal e preencher a tabela de auditoria
- [x] T3: Verificar GAP-3 (integrations bypass) e documentar decisao
- [x] T4: Adicionar comentario de dead code em `settings/notifications/route.ts`
- [x] T5: Executar testes de confirmacao (AC 18.1.5.5)
- [x] T6: `npm run typecheck && npm run lint`

---

## Arquivos Envolvidos

| Arquivo | Mudanca |
|---------|---------|
| `src/app/api/portal/settings/notifications/route.ts` | Adicionar comentario de dead code |
| Esta story | Atualizar tabela de auditoria com resultados finais |

---

## Riscos

- **R1:** Auditoria pode revelar rotas nao mapeadas que precisam de fix. Mitiga: criar sub-stories se necessario.
- **R2:** GAP-3 (`integrations` bypass) nao e resolvido nesta story. Documentar para Story 18.2.

---

## CodeRabbit Integration

### Specialized Agents
- **Security Agent:** Review completo de scoping em todas as rotas portal

### Review Focus Areas
- Confirmar que NENHUMA rota com `adminClient` falta filtro por `client_id`
- Verificar que rotas com `supabase` dependem de policies que existem (nao foram dropadas)

---

## Change Log

| Data | Autor | Mudanca |
|------|-------|---------|
| 2026-03-05 | @sm | Story criada com inventario completo de 33 handlers em 22 arquivos |
| 2026-03-05 | @dev | Auditoria completa: 22 arquivos lidos, 33 handlers verificados. 0 FIX NEEDED, 1 DEAD CODE (notifications), 32 OK. Comentario dead code adicionado. typecheck + lint OK. |
