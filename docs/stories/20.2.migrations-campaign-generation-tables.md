# Story 20.2 — Migrations Supabase: Tabelas Campaign Generation

## Metadados

| Campo      | Valor                                        |
|------------|----------------------------------------------|
| Prioridade | P0                                           |
| Sprint     | Atual                                        |
| Assignee   | @dev                                         |
| Revisao    | @sm                                          |
| Status     | Done                                        |
| Epic       | 20 - Gerador de Campanhas (Copy Generation)  |
| Fase       | DB                                           |

---

## Story

**Como** desenvolvedor implementando as APIs de campaign generation,
**Quero** as tabelas `campaign_generations` e `campaign_generation_stores` criadas no Supabase,
**Para que** as API routes possam persistir e consultar dados de campanhas.

---

## Contexto

As API routes (stories 20.3-20.5) dependem dessas tabelas. Os testes TDD mockam o Supabase mas a estrutura real precisa existir para deploy. O schema segue o PRD.

Seguranca: queries filtram por `client_id` do portal user. Writes usam `adminClient` (service_role). RLS policies devem permitir SELECT para portal users filtrando por client_id.

---

## Acceptance Criteria

### 20.2.1 — Criar tabela campaign_generations

- [x] Criar migration `supabase/migrations/20260305_campaign_generations.sql`
- [x] Tabela `campaign_generations` com colunas:
  - `id` UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - `client_id` UUID NOT NULL REFERENCES clients(id)
  - `name` VARCHAR(255) NOT NULL
  - `date` DATE NOT NULL
  - `reference_doc_url` TEXT NULL
  - `drive_folder_id` TEXT NULL
  - `drive_folder_url` TEXT NULL
  - `status` TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'processing', 'done'))
  - `created_at` TIMESTAMPTZ NOT NULL DEFAULT now()
  - `updated_at` TIMESTAMPTZ NOT NULL DEFAULT now()

### 20.2.2 — Criar tabela campaign_generation_stores

- [x] Tabela `campaign_generation_stores` com colunas:
  - `id` UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - `generation_id` UUID NOT NULL REFERENCES campaign_generations(id) ON DELETE CASCADE
  - `store_id` UUID NOT NULL REFERENCES client_stores(id)
  - `status` TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'generating', 'done', 'error'))
  - `version` INTEGER NOT NULL DEFAULT 1
  - `error_message` TEXT NULL
  - `generated_at` TIMESTAMPTZ NULL
  - `created_at` TIMESTAMPTZ NOT NULL DEFAULT now()

### 20.2.3 — Indices

- [x] Indice em `campaign_generations(client_id)` — consultas do portal filtram por client
- [x] Indice em `campaign_generation_stores(generation_id)` — lookup de lojas por campanha
- [x] Indice unico composto `campaign_generation_stores(generation_id, store_id)` — evitar duplicatas

### 20.2.4 — RLS Policies

- [x] Habilitar RLS em ambas as tabelas
- [x] Policy SELECT em `campaign_generations`: portal users podem ver apenas campanhas do proprio client_id
- [x] Policy SELECT em `campaign_generation_stores`: portal users podem ver stores de campanhas que pertencem ao client
- [x] NAO criar policies de INSERT/UPDATE para anon — writes sao via adminClient (service_role)

### 20.2.5 — Trigger updated_at

- [x] Criar trigger `set_updated_at` em `campaign_generations` para atualizar `updated_at` automaticamente

### 20.2.6 — Registrar env vars novas

- [x] Adicionar `N8N_CAMPAIGNS_WEBHOOK_URL` em `.env.example` (com valor placeholder)
- [x] Adicionar `N8N_WEBHOOK_SECRET` em `.env.example` (com valor placeholder)

---

## Arquitetura Tecnica

### Migration SQL (referencia)

```sql
-- campaign_generations
CREATE TABLE IF NOT EXISTS campaign_generations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  client_id UUID NOT NULL REFERENCES clients(id),
  name VARCHAR(255) NOT NULL,
  date DATE NOT NULL,
  reference_doc_url TEXT,
  drive_folder_id TEXT,
  drive_folder_url TEXT,
  status TEXT NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'processing', 'done')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- campaign_generation_stores
CREATE TABLE IF NOT EXISTS campaign_generation_stores (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  generation_id UUID NOT NULL REFERENCES campaign_generations(id) ON DELETE CASCADE,
  store_id UUID NOT NULL REFERENCES client_stores(id),
  status TEXT NOT NULL DEFAULT 'pending'
    CHECK (status IN ('pending', 'generating', 'done', 'error')),
  version INTEGER NOT NULL DEFAULT 1,
  error_message TEXT,
  generated_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Indices
CREATE INDEX idx_campaign_generations_client_id ON campaign_generations(client_id);
CREATE INDEX idx_campaign_generation_stores_generation_id ON campaign_generation_stores(generation_id);
CREATE UNIQUE INDEX idx_campaign_generation_stores_gen_store ON campaign_generation_stores(generation_id, store_id);

-- RLS
ALTER TABLE campaign_generations ENABLE ROW LEVEL SECURITY;
ALTER TABLE campaign_generation_stores ENABLE ROW LEVEL SECURITY;

-- Trigger updated_at (reutilizar funcao existente se houver, senao criar)
CREATE TRIGGER set_campaign_generations_updated_at
  BEFORE UPDATE ON campaign_generations
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

---

## File List

| Arquivo | Acao | Descricao |
|---------|------|-----------|
| `supabase/migrations/20260305_campaign_generations.sql` | CRIAR | Migration com tabelas, indices, RLS, trigger |
| `.env.example` | MODIFICAR | Adicionar N8N_CAMPAIGNS_WEBHOOK_URL e N8N_WEBHOOK_SECRET |

## Dependencias

- Nenhuma (pode ser feita em paralelo com 20.1)

## Riscos

- Verificar se funcao `update_updated_at_column()` ja existe no projeto. Se nao, criar na migration.
- FK para `clients(id)` e `client_stores(id)` — essas tabelas devem existir.

## CodeRabbit Integration

### Specialized Agents
- **DB Migration Agent**: Verificar SQL syntax, FK constraints, indices

### Review Focus Areas
- RLS habilitado em ambas tabelas
- ON DELETE CASCADE na FK de stores
- Indice unico composto para evitar duplicatas

---

## Definition of Done

- [x] Migration aplicada sem erros: `supabase db push` ou via MCP
- [x] Tabelas e indices confirmados
- [x] RLS habilitado em ambas tabelas
- [x] Env vars registradas em `.env.example`

---

## Change Log

| Data | Mudanca | Autor |
|------|---------|-------|
| 2026-03-05 | Story criada | @sm |
| 2026-03-05 | Remover 'error' do CHECK status campanha, add AC env vars, add DoD | @sm |
