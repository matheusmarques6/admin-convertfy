# Story 28.2 — Fix portal login window.location em useState

## Metadados

| Campo      | Valor                                    |
|------------|------------------------------------------|
| Prioridade | P1 (Alta)                                |
| Sprint     | Atual                                    |
| Assignee   | @dev                                     |
| Revisao    | @qa                                      |
| Status     | Done                                     |
| Epic       | 28 - Fix React Hydration Mismatch (#418) |
| Fase       | Bugfix                                   |

---

## Story

**Como** usuario do portal que acessa `/portal/login?error=session_expired`,
**Quero** que a pagina carregue sem erro de hydration,
**Para que** a mensagem de erro apareca corretamente sem crash no console.

---

## Contexto

Em `src/app/portal/login/page.tsx` (linhas 22-29), o `useState` initializer le `window.location.search`:

```typescript
const [error, setError] = useState<string | null>(() => {
    if (typeof window !== "undefined") {
      const params = new URLSearchParams(window.location.search)
      const errorParam = params.get("error")
      return errorParam ? ERROR_MESSAGES[errorParam] || null : null
    }
    return null
})
```

- **Server**: retorna `null` (branch `typeof window === "undefined"`)
- **Client**: retorna `"Sessao expirada..."` se houver `?error=session_expired`

O `typeof window` guard previne crash mas **nao previne hydration mismatch** — React compara o HTML do server (sem Alert de erro) com o client (com Alert visivel).

### Arquivos afetados

- `src/app/portal/login/page.tsx` (linhas 22-29)

---

## Acceptance Criteria

### 28.2.1 — Mover leitura de URL params para useEffect

- [x] Inicializar `error` state como `null` (sem funcao initializer)
- [x] Adicionar `useEffect` que le `window.location.search`, extrai o param `error`, e chama `setError()` com a mensagem correspondente
- [x] O `useEffect` deve ter dependency array `[]` (rodar apenas na montagem)
- [x] Manter o mapeamento `ERROR_MESSAGES` existente

### 28.2.2 — Validacao

- [ ] Testar `/portal/login?error=session_expired` — mensagem aparece apos mount sem erro #418
- [ ] Testar `/portal/login` sem params — sem mensagem, sem erros
- [x] `npm run lint` e `npm run typecheck` passam

---

## File List

| Arquivo | Acao |
|---------|------|
| `src/app/portal/login/page.tsx` | Modificar — mover window.location para useEffect |
