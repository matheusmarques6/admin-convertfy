---
Prioridade: P1
Sprint: Backlog
Assignee: "@dev (Dex)"
Revisao: "@architect"
Status: In Review
Epic: AE - Image Niche-Adaptive
Fase: QA / Vision
Estimate: M
---

# Story AE-15 — QA cascade: alt_text grátis + vision condicional + 4 novos `QaIssueType` pra imagem

## User Story

**Como** plataforma de qualidade,
**quero** que o QA agent valide tambem a imagem hero do email (nicho coerente, paleta, overlay reserve, cena),
**para que** falhas tipo "L'Hombre + hero de cueca" sejam bloqueadas antes do designer ver — sem inflar custo (alt_text gratis pega 80%, Claude vision so se passou o filtro).

---

## Contexto

Hoje (após AE-14):
- QA agent (`src/lib/agents/chains/qa.chain.ts`, AE-5) valida HTML + blocks + briefing — **mas nao olha pra imagem**
- `QaIssueType` enum tem 8 valores textuais (`spam_score_alto`, `html_invalido`, etc) — nenhum tipo dedicado a imagem
- `phase2-runner` ja gera imagem antes do QA: tem `image_url` em `email_blocks.content.image_url`
- `productRefDescriptionFallback` e o prompt mestre AE-14 ja injetam descricao textual rica do produto

Esta story implementa o QA cascade definido no plano §5 (`/root/.claude/plans/muito-bom-agora-vamos-playful-marble.md`):

**Etapa 1 — alt_text grátis** (sempre roda):
- Gera `image_alt` descritivo no momento da geracao da imagem (extrai do proprio prompt — ja temos `PRODUTO_HEROI`, `CENARIO`, `MOOD`)
- QA agent (LLM textual existente) compara `image_alt` com `{PRODUTO_HEROI}` esperado — custo zero adicional (LLM ja roda)
- Issue type novo: `image_nicho_mismatch` (severity high se threshold=high)

**Etapa 2 — vision condicional** (só dispara se filtro determinístico passa):
- Filtro determinístico: se Etapa 1 nao pegou nada óbvio E `EMAIL_QA_VISION_ENABLED=true`
- Manda `image_url` + `{NICHO}` + `{PRODUTO_HEROI}` + paleta pra Claude Sonnet 4.6 com vision
- Issue types: `image_paleta_off`, `image_overlay_reserva_ausente`, `image_cena_inadequada`
- Custo ~1.5¢/email (so quando passar o filtro)

---

## Acceptance Criteria

### AC AE-15.1 — Novos `QaIssueType` em `email-generation.ts`
- [x] Extender `QaIssueType` union em `src/types/email-generation.ts` com 4 valores novos:
  - `image_nicho_mismatch` (Etapa 1 — alt_text vs produto esperado)
  - `image_paleta_off` (Etapa 2 vision)
  - `image_overlay_reserva_ausente` (Etapa 2 vision)
  - `image_cena_inadequada` (Etapa 2 vision)
- [x] Total: 12 issue types (8 antigos + 4 novos)
- [x] Zod schema atualizado (`QA_ISSUE_TYPES` no `qa.chain.ts`)

### AC AE-15.2 — Generation: `image_alt` descritivo no `image_blocks.content`
- [x] Em `src/lib/agents/image/resolve-block-prompt.service.ts`: novo helper exportado `buildImageAlt(vars)` produz `"{PRODUTO_HEROI} em {CENARIO}, mood {MOOD}"` (trunc 200 chars)
- [x] `BlockPromptResolution.imageAlt` retornado nos endpoints AE-16 (preview na UI)
- [x] `phase2-runner.service.ts` usa `buildImageAlt(promptVars)` no UPDATE de `email_blocks.content.image_alt` (substitui `blk.label`)
- [x] Compat retroativa: fallback pra `blk.label` se helper retornar string vazia; blocks antigos persistidos com `image_alt = blk.label` continuam validos

### AC AE-15.3 — QA Etapa 1 (alt_text — grátis)
- [x] Migration `20260625_qa_agent_image_issue_types.sql`: UPDATE no `system_prompt` da v1 ativa do QA agent adicionando instrução sobre `image_nicho_mismatch` + os 4 novos issue types
- [x] `blocks_json` ja inclui `image_alt` (campo do `content` JSONB) — LLM ja recebe via template existente
- [x] Custo zero adicional (LLM ja roda no QA)
- [x] Issue `image_nicho_mismatch` bloqueia ready se threshold=high (severity=high obrigatorio via prompt)
- [ ] **Validação manual pendente**: aplicar migration em staging + rodar pipeline com caso L'Hombre+cueca confirmando bloqueio

### AC AE-15.4 — QA Etapa 2 (vision Claude Sonnet 4.6 condicional)
- [x] Feature flag: `EMAIL_QA_VISION_ENABLED=false` em `.env.example`
- [x] Nova funcao `runQaVisionCheck` em `src/lib/agents/chains/qa-vision.chain.ts`
- [x] Usa Claude Sonnet 4.6 (LangChain `ChatAnthropic.invoke` com content array multimodal — image_url + text)
- [x] System prompt em 3 dimensões: paleta, overlay reserve, cena
- [x] Output Zod-validated; falhas (parse/zod/throw) viram `issues:[]` graceful
- [ ] Custo logado em `email_generation_runs.cost_cents` — **diferido**: por ora `costCents` retornado da função (1.5¢ estimado) não é loggado em telemetry separada; o `qa.chain.ts` continua loggando 1 run agregado com custo do textual. Refinar quando ops ligar vision em prod
- [x] Dispara somente quando `EMAIL_QA_VISION_ENABLED='true'` E Etapa 1 nao reprovou E bloco tem `image_url`
- [ ] **Validação manual pendente**: precisa ANTHROPIC_API_KEY real + caso vision-only (paleta off) pra confirmar comportamento end-to-end

### AC AE-15.5 — Integração no `qa.chain.ts` `runQaAgent`
- [x] Apos LLM textual: itera blocos hero/image com `image_url`, cap em 3 paralelos via `Promise.allSettled`, mescla issues
- [x] Timeout 10s pro vision via `AbortController` (vs 15s do textual)
- [x] Fallback: vision throw/timeout → 0 issues + log.warn; passed do textual preservado

### AC AE-15.6 — `phase2-runner` continua funcionando com cascade
- [x] phase2-runner persiste `image_alt` descritivo **antes** de chamar `runQaAgent` (UPDATE em `email_blocks.content` no Step 1)
- [x] Sem mudanca de ordem do pipeline (image → html → qa)
- [x] Issue `image_nicho_mismatch` severity=high → `qaResult.passed=false` → status=failed (fluxo AE-3.5 preservado)

### AC AE-15.7 — Testes
- [x] `qa.chain.test.ts`: +6 testes cascade — vision OFF (default skip), vision ON sem mismatch, mismatch high já bloqueia (skip vision), sem image_url (skip vision), vision throw graceful, cap 3 paralelos
- [x] `qa-vision.chain.test.ts` (novo, 8 testes): JSON OK, paleta off, fences markdown, content array Anthropic, JSON inválido, Zod fail, vision throw, item Zod-inválido em array
- [ ] `resolve-block-prompt.test.ts`: nao estendido — `buildImageAlt` ja coberto indiretamente via testes do `runQaAgent` cascade. Adicionar unit test futuro pro helper
- [x] Integration: 178/178 testes `src/lib/agents/` verdes (vision OFF default)

### AC AE-15.8 — Compat retroativa
- [x] Feature flag vision OFF por default: zero impacto observavel
- [x] 178 testes verdes (sem regressao)
- [x] Typecheck OK
- [x] Lint 0 warnings nos arquivos novos/modificados

---

## Tarefas

- [ ] Estender `src/types/email-generation.ts` com 4 issue types novos
- [ ] Estender Zod schema em `qa.chain.ts`
- [ ] Estender `resolve-block-prompt.service.ts` com `image_alt` descritivo (e helper compartilhado entre prompt+alt)
- [ ] Estender `phase2-runner.service.ts` pra persistir `image_alt` novo
- [ ] Estender system_prompt do QA agent (em DB ou no fallback hardcoded) com instrução sobre image_nicho_mismatch
- [ ] Criar `src/lib/agents/chains/qa-vision.chain.ts` (`runQaVisionCheck`) + testes
- [ ] Integrar cascade em `runQaAgent` (chamada vision condicional após etapa textual)
- [ ] Adicionar `EMAIL_QA_VISION_ENABLED=false` em `.env.example`
- [ ] Atualizar testes

---

## Dev Notes

### Por que cascade (textual primeiro, vision condicional)?
- 80% dos casos óbvios (L'Hombre+cueca) pegam via comparação textual `image_alt` vs `PRODUTO_HEROI` — custo zero
- 20% restantes (nuances de paleta, overlay, cena) precisam vision — custo 1.5¢/email
- Cascade evita gastar $ em casos triviais

### Por que Claude Sonnet 4.6 com vision?
- LangChain `ChatAnthropic` suporta multimodal (texto + image_url) desde Claude 3
- Custo: ~$3/MTok input, ~$15/MTok output. Tipico: 1KB input + 0.5KB output → ~$0.005-0.015/call
- Output JSON-estruturado via tool_use ou prompt strict

### `image_alt` formato
Curto, factual, pra o QA textual comparar:
```
"Capa banco couro preto em interior de carro premium, mood masculino sobrio"
```
Não confundir com `image_alt` de acessibilidade HTML (esse fica no render final do email, pode ser mais conciso).

### Vision: o que avaliar?
1. **Produto coerente com NICHO+PRODUTO_HEROI**: "essa imagem mostra capa de banco?" (sim/não)
2. **Paleta da marca**: "as cores dominantes batem com paleta de hex {PALETA_1}+{PALETA_2}?"
3. **Overlay reserve quando esperado**: "terço inferior é mais escuro/limpo pra texto?"
4. **Cena coerente**: "ambiente compatível com posicionamento (premium/popular/medio)?"

Cada um vira issue type específico (severity ajustável via env).

### Threshold de bloqueio
- Já existe `EMAIL_QA_BLOCK_SEVERITY` (AE-5). Default `high`.
- `image_nicho_mismatch` deve ser `high` (bloqueia). Outros 3 (paleta/overlay/cena) ficam `medium` por default (não bloqueiam, mas aparecem em UI).

---

## Reuso de padrões existentes

- `runQaAgent` em `qa.chain.ts` (AE-5)
- `EMAIL_QA_BLOCK_SEVERITY` env var (AE-5)
- LangChain `ChatAnthropic` com multimodal input (AE-13 product_ref usa OpenRouter, mas Anthropic vision é diferente — verificar)
- `logGenerationRun` pra telemetria (`agent='qa'`, `metadata.stage='vision'`)
- `productRefDescriptionFallback` (AE-13) — não relacionado mas mesmo pattern (helper puro)

---

## File List

### A criar
- `src/lib/agents/chains/qa-vision.chain.ts`
- `src/lib/agents/chains/qa-vision.chain.test.ts`

### A modificar
- `src/types/email-generation.ts` (4 issue types novos)
- `src/lib/agents/chains/qa.chain.ts` (Zod schema + integração cascade vision)
- `src/lib/agents/chains/qa.chain.test.ts` (testes novos: image_nicho_mismatch + cascade)
- `src/lib/agents/image/resolve-block-prompt.service.ts` (image_alt descritivo)
- `src/lib/agents/phase2-runner.service.ts` (image_alt descritivo no UPDATE)
- `.env.example` (`EMAIL_QA_VISION_ENABLED=false`)
- Seed AE-5 do QA agent (atualizar `system_prompt` se vai pelo DB; senao só hardcoded)

---

## Dependencias

- **Bloqueado por**: AE-5 (QA agent existente), AE-10..14 (helpers + prompts)
- **Bloqueia**: nada — última story do épico

---

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|---------------|-----------|
| LangChain ChatAnthropic vision não suporta image_url externa | Baixa | Verificar empiricamente; fallback: baixar imagem e enviar base64 (Anthropic suporta) |
| Vision custo escala mal | Media | Feature flag OFF + cascade (só 20% dos emails) → controlado |
| `image_alt` muito longo polui telemetria | Baixa | Truncate 200 chars no persist |
| Vision rejeita imagem (ex: NSFW filter) | Baixa | Fallback graceful: log warn, segue sem block |
| 4 issue types novos quebram seeds existentes do QA agent (AE-5) | Baixa | Atualizar seed em migration nova OU patch no system_prompt em runtime |

---

## Change Log

| Data | Autor | Descricao |
|------|-------|-----------|
| 2026-05-30 | @architect | Story criada apos AE-16 — ultima do epico |
| 2026-05-30 | @dev (Dex) | Implementacao: 4 issue types, helper `buildImageAlt`, `qa-vision.chain.ts` (Claude Sonnet 4.6 multimodal), cascade no `runQaAgent` (cap 3, timeout 10s, graceful fail), migration `20260625_qa_agent_image_issue_types.sql`, `EMAIL_QA_VISION_ENABLED=false`. 14 novos testes verdes (178/178 agents). Status → In Review. Pendente: validação manual em staging (caso L'Hombre+cueca) e telemetria de custo separada da Etapa 2. |
