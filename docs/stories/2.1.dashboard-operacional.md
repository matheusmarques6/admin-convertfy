# Story 2.1 - Dashboard Operacional (Contas Agente)

**Prioridade:** Alta
**Sprint:** Current
**Assignee:** @dev
**Revisão:** @sm (River)
**Status:** Draft

---

## Contexto

A dashboard atual (`/dashboard`) é focada em administração: receita, MRR, pipeline de vendas, billing Asaas, Klaviyo. Ela deve ser restrita apenas a contas **admin/owner**. Precisamos criar uma **Dashboard Operacional** que será exibida para todas as contas **agente** (membros da organização que NÃO são admin/owner).

---

## Objetivo

Criar uma dashboard operacional com visão focada no dia-a-dia do agente, contendo:

1. **Resultado Total** (receita das lojas do agente)
2. **Overview Cards** (métricas rápidas operacionais)
3. **Novos Clientes** (clientes recentemente adicionados)
4. **Novas Lojas para Implementação** (lojas em onboarding/setup)
5. **Alertas Recentes** (contexto operacional)
6. **Overview Operacional** (tasks, onboardings, atividades)

---

## Critérios de Aceitação

- [ ] Admin/Owner acessando `/dashboard` vê a dashboard admin atual (sem alterações)
- [ ] Agente acessando `/dashboard` é redirecionado para `/dashboard/operational`
- [ ] Dashboard operacional exibe APENAS dados das lojas/clientes que o agente tem acesso via `agent_store_access`
- [ ] Componente Resultado Total funciona filtrado pelas lojas do agente
- [ ] Overview cards mostram métricas corretas do escopo do agente
- [ ] Lista de novos clientes mostra clientes recentes do escopo do agente
- [ ] Lista de novas lojas para implementação mostra lojas em onboarding
- [ ] Alertas recentes são filtrados pelo escopo do agente
- [ ] Overview operacional mostra tasks e onboardings do agente
- [ ] Layout responsivo (mobile, tablet, desktop)
- [ ] Animações seguem o padrão existente (Framer Motion)
- [ ] Skeleton loaders durante carregamento
- [ ] Empty states para cada seção quando sem dados

---

## Arquitetura Técnica

### 1. Estrutura de Arquivos a Criar

```
src/
├── app/(dashboard)/dashboard/
│   ├── page.tsx                          # MODIFICAR - adicionar redirect condicional
│   └── operational/
│       └── page.tsx                      # CRIAR - página da dashboard operacional
├── components/dashboard/
│   ├── operational/                      # CRIAR - pasta de componentes operacionais
│   │   ├── operational-metrics.tsx       # Overview cards
│   │   ├── new-clients.tsx              # Novos clientes
│   │   ├── new-stores.tsx               # Novas lojas para implementação
│   │   ├── operational-alerts.tsx       # Alertas recentes
│   │   └── operational-overview.tsx     # Overview operacional
│   └── total-revenue-banner.tsx         # MODIFICAR - aceitar filtro por store_ids
└── app/api/dashboard/
    ├── operational/
    │   └── route.ts                     # CRIAR - API de dados operacionais
    └── total-revenue/
        └── route.ts                     # MODIFICAR - aceitar filtro por store_ids
```

---

### 2. Roteamento Condicional (FASE 1)

#### Arquivo: `src/app/(dashboard)/dashboard/page.tsx`

**O que modificar:**
O componente atual é um `async function DashboardPage()` server component. Precisamos adicionar lógica de redirect no topo ANTES de buscar dados.

**Lógica de implementação:**

```typescript
// No TOPO da função DashboardPage(), ANTES de getDashboardData():

import { redirect } from "next/navigation"
import { createClient } from "@/lib/supabase/server"

export default async function DashboardPage() {
  // 1. Verificar se usuário é admin/owner
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) redirect("/login")

  // 2. Buscar profile para checar role
  const { data: profile } = await supabase
    .from("profiles")
    .select("role")
    .eq("id", user.id)
    .single()

  const isAdmin = profile?.role === "admin"

  // 3. Se não é admin, verificar se é owner da org
  if (!isAdmin) {
    const { data: orgMember } = await supabase
      .from("org_members")
      .select("role")
      .eq("profile_id", user.id)
      .eq("is_active", true)
      .limit(1)
      .single()

    const isOwner = orgMember?.role === "owner"

    // 4. Se NÃO é admin NEM owner → redirecionar
    if (!isOwner) {
      redirect("/dashboard/operational")
    }
  }

  // 5. Se chegou aqui, é admin/owner → mostrar dashboard admin normal
  const data = await getDashboardData()
  // ... resto do componente atual sem alterações
}
```

**IMPORTANTE:** NÃO alterar NADA da dashboard admin existente. Apenas adicionar o bloco de redirect no topo.

---

### 3. Página Dashboard Operacional (FASE 1)

#### Arquivo: `src/app/(dashboard)/dashboard/operational/page.tsx`

**Server Component** que busca todos os dados necessários e passa para os componentes client.

```typescript
import { redirect } from "next/navigation"
import { createClient } from "@/lib/supabase/server"
import { AnimatedContainer, AnimatedItem } from "@/components/ui/animated-container"
import { TotalRevenueBanner } from "@/components/dashboard/total-revenue-banner"
import { OperationalMetrics } from "@/components/dashboard/operational/operational-metrics"
import { NewClients } from "@/components/dashboard/operational/new-clients"
import { NewStores } from "@/components/dashboard/operational/new-stores"
import { OperationalAlerts } from "@/components/dashboard/operational/operational-alerts"
import { OperationalOverview } from "@/components/dashboard/operational/operational-overview"
import { Skeleton } from "@/components/ui/skeleton"
import { Suspense } from "react"
import type { DashboardAlert } from "@/types"

export const dynamic = "force-dynamic"

async function getOperationalData() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) redirect("/login")

  // 1. Buscar org_member_id do agente
  const { data: orgMember } = await supabase
    .from("org_members")
    .select("id, role")
    .eq("profile_id", user.id)
    .eq("is_active", true)
    .limit(1)
    .single()

  if (!orgMember) redirect("/login")

  // Se é admin/owner, redirecionar para dashboard admin
  if (orgMember.role === "owner") {
    // Verificar profile.role também
    const { data: profile } = await supabase
      .from("profiles")
      .select("role")
      .eq("id", user.id)
      .single()

    if (profile?.role === "admin" || orgMember.role === "owner") {
      redirect("/dashboard")
    }
  }

  // 2. Buscar lojas que o agente tem acesso
  const { data: storeAccess } = await supabase
    .from("agent_store_access")
    .select(`
      store_id,
      can_view,
      can_edit,
      can_manage_onboarding,
      can_manage_campaigns,
      can_manage_reports,
      store:client_stores(
        id,
        store_name,
        store_url,
        platform,
        is_active,
        client_id,
        created_at,
        client:clients(id, name, email, status, health_score, created_at)
      )
    `)
    .eq("org_member_id", orgMember.id)
    .eq("can_view", true)

  if (!storeAccess || storeAccess.length === 0) {
    return {
      storeIds: [],
      clientIds: [],
      metrics: { activeClients: 0, activeStores: 0, pendingTasks: 0, activeOnboardings: 0 },
      newClients: [],
      newStores: [],
      alerts: [],
      recentActivities: [],
      onboardings: [],
    }
  }

  // 3. Extrair IDs únicos
  const storeIds = storeAccess.map(a => {
    const store = Array.isArray(a.store) ? a.store[0] : a.store
    return store?.id
  }).filter(Boolean) as string[]

  const clientIds = [...new Set(
    storeAccess.map(a => {
      const store = Array.isArray(a.store) ? a.store[0] : a.store
      const client = store?.client
      const c = Array.isArray(client) ? client[0] : client
      return c?.id
    }).filter(Boolean) as string[]
  )]

  // 4. Buscar dados em paralelo
  const now = new Date()
  const thirtyDaysAgo = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000)

  const [
    { data: newClientsData },
    { data: newStoresData },
    { data: onboardingsData },
    { data: tasksData },
    { data: activitiesData },
    { data: expiringContracts },
    { data: lowHealthClients },
    { data: overdueCharges },
  ] = await Promise.all([
    // Novos clientes (últimos 30 dias, do escopo do agente)
    supabase
      .from("clients")
      .select("id, name, email, status, health_score, created_at, owner_id")
      .in("id", clientIds)
      .gte("created_at", thirtyDaysAgo.toISOString())
      .order("created_at", { ascending: false })
      .limit(10),

    // Novas lojas (em onboarding ou recém-criadas)
    supabase
      .from("client_stores")
      .select(`
        id, store_name, store_url, platform, is_active, created_at,
        client:clients(id, name, status),
        onboardings:client_onboardings(id, status, progress_percent, target_completion_date)
      `)
      .in("id", storeIds)
      .order("created_at", { ascending: false })
      .limit(10),

    // Onboardings ativos
    supabase
      .from("client_onboardings")
      .select(`
        id, status, progress_percent, target_completion_date, started_at,
        client:clients(id, name),
        store:client_stores(id, store_name, platform),
        steps:client_onboarding_steps(id, status, name)
      `)
      .in("store_id", storeIds)
      .in("status", ["not_started", "in_progress", "paused"])
      .order("created_at", { ascending: false })
      .limit(10),

    // Tasks pendentes do agente
    supabase
      .from("tasks")
      .select("id, title, status, priority, due_date, type")
      .eq("assignee_id", orgMember.id)
      .in("status", ["pending", "in_progress", "blocked", "review"])
      .order("due_date", { ascending: true })
      .limit(20),

    // Atividades recentes (dos clientes do agente)
    supabase
      .from("activities")
      .select("id, type, description, created_at, client:clients(name), profile:profiles(name)")
      .in("client_id", clientIds)
      .order("created_at", { ascending: false })
      .limit(8),

    // Contratos expirando (dos clientes do agente)
    supabase
      .from("contracts")
      .select("id, plan_name, end_date, client_id")
      .eq("status", "active")
      .in("client_id", clientIds)
      .not("end_date", "is", null)
      .lte("end_date", new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000).toISOString())
      .order("end_date", { ascending: true })
      .limit(5),

    // Clientes com health score baixo
    supabase
      .from("clients")
      .select("id, name, health_score")
      .in("id", clientIds)
      .eq("status", "active")
      .lt("health_score", 30)
      .order("health_score", { ascending: true })
      .limit(5),

    // Cobranças vencidas
    supabase
      .from("client_charges")
      .select("id, description, value, due_date, client_id")
      .in("client_id", clientIds)
      .eq("status", "overdue")
      .order("due_date", { ascending: true })
      .limit(5),
  ])

  // 5. Calcular métricas
  const activeClients = [...new Set(
    storeAccess
      .map(a => {
        const store = Array.isArray(a.store) ? a.store[0] : a.store
        const client = store?.client
        const c = Array.isArray(client) ? client[0] : client
        return c?.status === "active" ? c.id : null
      })
      .filter(Boolean)
  )].length

  const activeStores = storeAccess.filter(a => {
    const store = Array.isArray(a.store) ? a.store[0] : a.store
    return store?.is_active
  }).length

  const pendingTasks = tasksData?.length || 0

  const activeOnboardings = onboardingsData?.filter(
    o => o.status === "in_progress" || o.status === "not_started"
  ).length || 0

  // 6. Construir alertas
  const alerts: DashboardAlert[] = []

  // Mapa de nomes de clientes para alertas
  const clientNameMap = new Map<string, string>()
  storeAccess.forEach(a => {
    const store = Array.isArray(a.store) ? a.store[0] : a.store
    const client = store?.client
    const c = Array.isArray(client) ? client[0] : client
    if (c?.id && c?.name) clientNameMap.set(c.id, c.name)
  })

  overdueCharges?.forEach(c => {
    alerts.push({
      id: `overdue-${c.id}`,
      type: "payment_overdue",
      title: "Pagamento vencido",
      description: `${clientNameMap.get(c.client_id) || "Cliente"} - ${c.description || "Cobrança"} vencida em ${new Date(c.due_date).toLocaleDateString("pt-BR")}`,
      severity: "high",
    })
  })

  expiringContracts?.forEach(c => {
    alerts.push({
      id: `contract-${c.id}`,
      type: "contract_expiring",
      title: "Contrato expirando",
      description: `${clientNameMap.get(c.client_id) || "Cliente"} - ${c.plan_name} expira em ${new Date(c.end_date!).toLocaleDateString("pt-BR")}`,
      severity: "medium",
    })
  })

  lowHealthClients?.forEach(c => {
    alerts.push({
      id: `health-${c.id}`,
      type: "health_low",
      title: "Health score baixo",
      description: `${c.name} está com score ${c.health_score}/100`,
      severity: "low",
    })
  })

  return {
    storeIds,
    clientIds,
    metrics: {
      activeClients,
      activeStores,
      pendingTasks,
      activeOnboardings,
    },
    newClients: newClientsData || [],
    newStores: newStoresData || [],
    alerts,
    recentActivities: activitiesData || [],
    onboardings: onboardingsData || [],
  }
}

export default async function OperationalDashboardPage() {
  const data = await getOperationalData()

  return (
    <AnimatedContainer className="space-y-6">
      {/* Resultado Total - filtrado pelas lojas do agente */}
      <AnimatedItem>
        <TotalRevenueBanner storeIds={data.storeIds} />
      </AnimatedItem>

      {/* Overview Cards */}
      <AnimatedItem>
        <OperationalMetrics metrics={data.metrics} />
      </AnimatedItem>

      {/* Grid: Novos Clientes + Alertas */}
      <AnimatedItem>
        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-7">
          <div className="col-span-full lg:col-span-4 space-y-6">
            <NewClients clients={data.newClients} />
            <NewStores stores={data.newStores} />
          </div>
          <div className="col-span-full lg:col-span-3 space-y-6">
            <OperationalAlerts alerts={data.alerts} />
            <OperationalOverview
              activities={data.recentActivities}
              onboardings={data.onboardings}
              pendingTasks={data.metrics.pendingTasks}
            />
          </div>
        </div>
      </AnimatedItem>
    </AnimatedContainer>
  )
}
```

---

### 4. Componentes da Dashboard Operacional (FASE 2)

#### 4.1 `src/components/dashboard/operational/operational-metrics.tsx`

**Propósito:** 4 cards de métricas rápidas (igual ao `metrics.tsx` admin, mas com dados operacionais).

**Props:**
```typescript
interface OperationalMetricsProps {
  metrics: {
    activeClients: number
    activeStores: number
    pendingTasks: number
    activeOnboardings: number
  }
}
```

**Implementação:**
- Usar `GlowCard` com cores:
  - Clientes Ativos → `color="primary"` + ícone `Users`
  - Lojas Ativas → `color="success"` + ícone `Store`
  - Tasks Pendentes → `color="warning"` + ícone `ClipboardList`
  - Onboardings Ativos → `color="info"` + ícone `Rocket`
- Layout: `grid gap-4 md:grid-cols-2 lg:grid-cols-4`
- Cada card mostra número grande + label + ícone com background colorido

**Referência visual:** Seguir exatamente o padrão de `src/components/dashboard/metrics.tsx` (linhas 1-130), substituindo os dados financeiros por dados operacionais.

---

#### 4.2 `src/components/dashboard/operational/new-clients.tsx`

**Propósito:** Lista de clientes recentes (últimos 30 dias) que estão no escopo do agente.

**Props:**
```typescript
interface NewClientsProps {
  clients: Array<{
    id: string
    name: string
    email: string
    status: string
    health_score: number
    created_at: string
    owner_id?: string
  }>
}
```

**Implementação:**
- Card com título "Novos Clientes" + Badge com contagem
- Ícone `UserPlus` no header
- Lista scrollável (max 300px `ScrollArea`)
- Cada item mostra:
  - Avatar com iniciais do nome (componente `Avatar` existente)
  - Nome do cliente
  - Status badge colorido (usar `STATUS_COLORS` do dashboard/page.tsx)
  - Data de criação (usar função `timeAgo` do `recent-activity.tsx`)
  - Health score indicator (barra de progresso pequena)
- Link para `/clients/{id}` em cada item
- Empty state: "Nenhum novo cliente nos últimos 30 dias" com ícone `Users`

**Referência:** Seguir padrão visual de `src/components/dashboard/recent-activity.tsx`

---

#### 4.3 `src/components/dashboard/operational/new-stores.tsx`

**Propósito:** Lojas que precisam de implementação (em onboarding/setup).

**Props:**
```typescript
interface NewStoresProps {
  stores: Array<{
    id: string
    store_name: string
    store_url: string
    platform: string
    is_active: boolean
    created_at: string
    client: { id: string; name: string; status: string } | null
    onboardings: Array<{
      id: string
      status: string
      progress_percent: number
      target_completion_date: string | null
    }> | null
  }>
}
```

**Implementação:**
- Card com título "Lojas para Implementação" + Badge com contagem
- Ícone `Store` no header
- Lista scrollável (max 300px `ScrollArea`)
- Cada item mostra:
  - Ícone da plataforma (Shopify verde, Nuvemshop azul, WooCommerce roxo, Other cinza)
  - Nome da loja
  - Nome do cliente (subtitle)
  - Badge de plataforma (Shopify, Nuvemshop, etc.)
  - Barra de progresso do onboarding (se existir)
  - Status do onboarding: "Não iniciado", "Em progresso X%", "Pausado"
  - Data alvo de conclusão (se existir)
- Link para `/stores/{id}` em cada item
- Empty state: "Nenhuma loja pendente de implementação" com ícone `Store`
- **Destaque visual** para lojas sem onboarding iniciado (borda warning)

**Referência:** Seguir padrão visual de `src/components/dashboard/recent-activity.tsx` + `src/components/onboarding/store-onboarding-card.tsx`

---

#### 4.4 `src/components/dashboard/operational/operational-alerts.tsx`

**Propósito:** Alertas recentes filtrados pelo escopo do agente.

**Props:**
```typescript
interface OperationalAlertsProps {
  alerts: DashboardAlert[]  // Importar de @/types
}
```

**Implementação:**
- Reutilizar a lógica do `src/components/dashboard/alerts.tsx` existente
- Card com título "Alertas Recentes" + Badge com contagem por severity
- Ícone `AlertTriangle` no header
- Lista com ícone + severity badge + descrição
- Severity colors:
  - `high` → `destructive` badge (vermelho)
  - `medium` → `warning` badge (amarelo/laranja)
  - `low` → `secondary` badge (cinza)
- Alert type icons (COPIAR do alerts.tsx existente):
  - `payment_overdue` → `DollarSign`
  - `contract_expiring` → `FileText`
  - `health_low` → `HeartPulse`
  - `meeting_overdue` → `Calendar`
  - `report_pending` → `BarChart3`
- Empty state: "Sem alertas recentes" com ícone `CheckCircle2` (verde)
- ScrollArea max 300px

**Referência:** Copiar estrutura de `src/components/dashboard/alerts.tsx` (sem a parte de meetings)

---

#### 4.5 `src/components/dashboard/operational/operational-overview.tsx`

**Propósito:** Visão geral operacional com atividades recentes, status de onboardings e tasks.

**Props:**
```typescript
interface OperationalOverviewProps {
  activities: Array<{
    id: string
    type: string
    description: string
    created_at: string
    client: { name: string } | { name: string }[] | null
    profile: { name: string } | { name: string }[] | null
  }>
  onboardings: Array<{
    id: string
    status: string
    progress_percent: number
    target_completion_date: string | null
    client: { id: string; name: string } | null
    store: { id: string; store_name: string; platform: string } | null
    steps: Array<{ id: string; status: string; name: string }> | null
  }>
  pendingTasks: number
}
```

**Implementação:**
- Card com Tabs: "Atividades" | "Onboardings"
- **Tab Atividades:**
  - Reutilizar padrão de `src/components/dashboard/recent-activity.tsx`
  - Lista com ícone colorido + descrição + tempo relativo
  - ScrollArea max 250px
- **Tab Onboardings:**
  - Lista de onboardings ativos com:
    - Nome da loja + cliente
    - Barra de progresso circular ou linear (progress_percent)
    - Contagem de steps: "X/Y concluídos"
    - Status badge
    - Data alvo
  - ScrollArea max 250px
- Empty states para cada tab

**Referência:** `src/components/dashboard/recent-activity.tsx` + `src/components/dashboard/charts.tsx` (padrão de tabs)

---

### 5. Modificação do TotalRevenueBanner (FASE 2)

#### Arquivo: `src/components/dashboard/total-revenue-banner.tsx`

**O que modificar:**

Adicionar prop opcional `storeIds` para filtrar a busca.

```typescript
// ANTES:
interface TotalRevenueBannerProps {}

// DEPOIS:
interface TotalRevenueBannerProps {
  storeIds?: string[]  // Se fornecido, filtra apenas essas lojas
}
```

**Na função `loadData()`:**

```typescript
// ANTES:
const response = await fetch(url)

// DEPOIS:
// Se storeIds foi passado, enviar como query param
if (storeIds && storeIds.length > 0) {
  url.searchParams.set("store_ids", storeIds.join(","))
}
const response = await fetch(url)
```

**IMPORTANTE:** A dashboard admin continua chamando `<TotalRevenueBanner />` sem props (mostra tudo). A dashboard operacional chama `<TotalRevenueBanner storeIds={data.storeIds} />` (mostra só as lojas do agente).

---

### 6. Modificação da API Total Revenue (FASE 2)

#### Arquivo: `src/app/api/dashboard/total-revenue/route.ts`

**O que modificar:**

Adicionar suporte ao parâmetro `store_ids` para filtrar lojas.

```typescript
// No início da função GET:
const storeIdsParam = searchParams.get("store_ids")
const filterStoreIds = storeIdsParam ? storeIdsParam.split(",") : null

// Na query de lojas, APÓS buscar allStores:
// Se filterStoreIds está presente, filtrar:
if (filterStoreIds) {
  stores = stores.filter(s => filterStoreIds.includes(s.id))
}
```

**IMPORTANTE:** A API continua funcionando normalmente sem o parâmetro (retorna todas as lojas da org). O filtro é opcional.

---

### 7. Atualização da Sidebar (FASE 1)

#### Arquivo: `src/components/layout/sidebar.tsx`

**NÃO precisa alterar a sidebar.** O link do Dashboard já aponta para `/dashboard`, e o redirect server-side cuida do roteamento. O agente será automaticamente redirecionado para `/dashboard/operational`.

---

## Layout Visual Detalhado

```
┌─────────────────────────────────────────────────────────┐
│  RESULTADO TOTAL                                        │
│  ┌─────────────────────────────────────────────────────┐│
│  │ R$ XX.XXX,XX (animado)  [7d] [15d] [30d] [90d]    ││
│  │ Campanhas: R$ X.XXX  |  Fluxos: R$ X.XXX          ││
│  │ ▓▓▓▓▓▓▓ Loja A  R$ 5.000                          ││
│  │ ▓▓▓▓▓   Loja B  R$ 3.500                          ││
│  │ ▓▓▓     Loja C  R$ 2.000                          ││
│  └─────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│  OVERVIEW CARDS                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 12       │ │ 8        │ │ 5        │ │ 3        │  │
│  │ Clientes │ │ Lojas    │ │ Tasks    │ │ Onboard. │  │
│  │ Ativos   │ │ Ativas   │ │ Pendente │ │ Ativos   │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
├──────────────────────────┬──────────────────────────────┤
│  NOVOS CLIENTES          │  ALERTAS RECENTES            │
│  ┌──────────────────────┐│  ┌──────────────────────────┐│
│  │ 👤 João Silva        ││  │ ⚠ Pagamento vencido     ││
│  │    Ativo · 2d atrás  ││  │   Cliente X - R$ 500    ││
│  │ 👤 Maria Santos      ││  │ ⚠ Contrato expirando   ││
│  │    Onboarding · 5d   ││  │   Cliente Y - 15 dias   ││
│  │ 👤 Pedro Costa       ││  │ ℹ Health score baixo    ││
│  │    Prospect · 7d     ││  │   Cliente Z - 25/100    ││
│  └──────────────────────┘│  └──────────────────────────┘│
├──────────────────────────┤                              │
│  LOJAS P/ IMPLEMENTAÇÃO  │  OVERVIEW OPERACIONAL        │
│  ┌──────────────────────┐│  ┌──────────────────────────┐│
│  │ 🛒 Loja Premium      ││  │ [Atividades] [Onboard.] ││
│  │    Shopify · 45%     ││  │ ● Cliente criado - 2h   ││
│  │    ▓▓▓▓░░░░░░ prog.  ││  │ ● Pagamento - 5h       ││
│  │ 🛒 Store Digital     ││  │ ● Reunião - 1d         ││
│  │    Nuvemshop · 0%    ││  │ ● Deal ganho - 2d      ││
│  │    Não iniciado      ││  │                          ││
│  └──────────────────────┘│  └──────────────────────────┘│
└──────────────────────────┴──────────────────────────────┘
```

---

## Padrões Obrigatórios

### Imports e Ícones
Todos os ícones vêm de `lucide-react`. Ícones usados:
```typescript
import {
  Users, Store, ClipboardList, Rocket,        // Metrics
  UserPlus,                                     // New Clients
  AlertTriangle, DollarSign, FileText,          // Alerts
  HeartPulse, Calendar, BarChart3, CheckCircle2,
  ShoppingBag,                                  // Stores (plataformas)
  ExternalLink,                                 // Links
  Activity,                                     // Overview
} from "lucide-react"
```

### Componentes UI Base
```typescript
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Progress } from "@/components/ui/progress"
import { Avatar, AvatarFallback } from "@/components/ui/avatar"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { GlowCard } from "@/components/ui/glow-card"
import { Skeleton } from "@/components/ui/skeleton"
```

### Utilitários
```typescript
import { cn } from "@/lib/utils"
import { formatCurrency, formatNumber } from "@/lib/utils"  // Verificar se existem
```

### Diretiva
Todos os componentes dentro de `operational/` devem ser `"use client"` pois usam hooks, interatividade, etc.

A página `operational/page.tsx` é **server component** (async function, sem "use client").

### Cores do Status de Cliente
```typescript
const STATUS_COLORS: Record<string, string> = {
  active: "bg-green-500/10 text-green-500",
  prospect: "bg-blue-500/10 text-blue-500",
  onboarding: "bg-purple-500/10 text-purple-500",
  inactive: "bg-gray-500/10 text-gray-500",
  churned: "bg-red-500/10 text-red-500",
}

const STATUS_LABELS: Record<string, string> = {
  active: "Ativo",
  prospect: "Prospect",
  onboarding: "Onboarding",
  inactive: "Inativo",
  churned: "Churned",
}
```

### Cores da Plataforma de Loja
```typescript
const PLATFORM_CONFIG: Record<string, { label: string; color: string; icon: string }> = {
  shopify: { label: "Shopify", color: "bg-green-500/10 text-green-600", icon: "🟢" },
  nuvemshop: { label: "Nuvemshop", color: "bg-blue-500/10 text-blue-600", icon: "☁️" },
  woocommerce: { label: "WooCommerce", color: "bg-purple-500/10 text-purple-600", icon: "🟣" },
  other: { label: "Outra", color: "bg-gray-500/10 text-gray-600", icon: "⚪" },
}
```

### Status do Onboarding
```typescript
const ONBOARDING_STATUS: Record<string, { label: string; color: string }> = {
  not_started: { label: "Não Iniciado", color: "bg-gray-500/10 text-gray-500" },
  in_progress: { label: "Em Progresso", color: "bg-blue-500/10 text-blue-500" },
  paused: { label: "Pausado", color: "bg-yellow-500/10 text-yellow-500" },
  completed: { label: "Concluído", color: "bg-green-500/10 text-green-500" },
  cancelled: { label: "Cancelado", color: "bg-red-500/10 text-red-500" },
}
```

---

## Ordem de Implementação

### FASE 1 - Infraestrutura (implementar primeiro)
1. [x] Criar pasta `src/components/dashboard/operational/`
2. [x] Criar `src/app/(dashboard)/dashboard/operational/page.tsx` (server component com data fetching)
3. [x] Modificar `src/app/(dashboard)/dashboard/page.tsx` (adicionar redirect condicional)
4. [ ] Testar: admin vê dashboard admin, agente é redirecionado

### FASE 2 - Componentes (implementar em ordem)
5. [x] Criar `operational-metrics.tsx` (mais simples, base para testar layout)
6. [x] Criar `new-clients.tsx`
7. [x] Criar `new-stores.tsx`
8. [x] Criar `operational-alerts.tsx`
9. [x] Criar `operational-overview.tsx`
10. [x] Modificar `total-revenue-banner.tsx` (adicionar prop storeIds)
11. [x] Modificar `src/app/api/dashboard/total-revenue/route.ts` (adicionar filtro store_ids)

### FASE 3 - Polish e Testes
12. [ ] Testar com conta agente real (dados filtrados corretamente)
13. [ ] Testar empty states (agente sem lojas, sem clientes)
14. [ ] Testar responsividade (mobile, tablet, desktop)
15. [ ] Verificar skeleton loaders durante carregamento
16. [ ] Verificar animações (Framer Motion)
17. [ ] Run `npm run lint`
18. [ ] Run `npm run typecheck`

---

## Dados de Teste

Para testar a dashboard operacional, você precisa:

1. **Um usuário agente** (NÃO admin, NÃO owner) na tabela `org_members`
2. **Registros em `agent_store_access`** vinculando esse agente a lojas específicas
3. **Clientes com diferentes status** vinculados a essas lojas
4. **Pelo menos um `client_onboarding`** em progresso
5. **Algumas `activities`** dos clientes do agente
6. **Opcional:** contratos expirando, cobranças vencidas, health scores baixos

---

## Notas Importantes para o Dev

1. **NÃO alterar a dashboard admin** - apenas adicionar o redirect no topo
2. **Todos os dados devem ser filtrados** por `agent_store_access` - NUNCA mostrar dados fora do escopo
3. **O TotalRevenueBanner é reutilizado** - não criar um novo, apenas adicionar a prop
4. **Server Components vs Client Components** - a page é server, os componentes internos são client
5. **Supabase queries usam o client do server** (`createClient` de `@/lib/supabase/server`) na page
6. **Seguir os padrões visuais existentes** - GlowCard, AnimatedContainer, Framer Motion, mesmas cores
7. **Textos em português** - todos os labels, empty states, tooltips devem ser em PT-BR
8. **Edge case: `storeAccess` vazio** - retornar dados zerados, não quebrar

---

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `src/app/(dashboard)/dashboard/page.tsx` | MODIFICAR | Adicionar redirect para agentes |
| `src/app/(dashboard)/dashboard/operational/page.tsx` | CRIAR | Página da dashboard operacional |
| `src/components/dashboard/operational/operational-metrics.tsx` | CRIAR | Cards de métricas operacionais |
| `src/components/dashboard/operational/new-clients.tsx` | CRIAR | Lista de novos clientes |
| `src/components/dashboard/operational/new-stores.tsx` | CRIAR | Lojas para implementação |
| `src/components/dashboard/operational/operational-alerts.tsx` | CRIAR | Alertas recentes |
| `src/components/dashboard/operational/operational-overview.tsx` | CRIAR | Overview operacional |
| `src/components/dashboard/total-revenue-banner.tsx` | MODIFICAR | Adicionar prop storeIds |
| `src/app/api/dashboard/total-revenue/route.ts` | MODIFICAR | Adicionar filtro store_ids |

---

*Story criada por @sm (River) — removendo obstáculos para o dev fluir* 🌊
