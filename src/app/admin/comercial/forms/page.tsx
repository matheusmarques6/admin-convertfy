import { FormsLista } from "@/components/forms/forms-lista"

/**
 * /admin/comercial/forms — a lista de formulários do Comercial.
 *
 * A tela mora em `components/forms/forms-lista.tsx`; a régua do que cada
 * número diz (e quando diz "—") em `lib/forms/lista.ts`.
 */
export default function FormsListPage() {
  return <FormsLista />
}
